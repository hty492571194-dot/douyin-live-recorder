# 抖音直播监控 · 录制 / 字幕 / 礼物 对接说明书

> **面向**：需要对接本项目的其他 AI 智能体
> **项目根**：`/Users/a/Downloads/douyin-monitor`（代码内一律用 `BASE = os.path.dirname(os.path.abspath(__file__))` 定位，路径无关）
> **生成日期**：2026-09-08 · 全部字段与参数均从当前代码与真实数据样本核对
> **配套文档**：`项目框架说明书.md`（全局架构）、`AI_AGENT_GUIDE.md`（代码导航）

---

## 0. 三十秒速览

| 能力 | 入口模块 | 产出物 | 是否依赖登录态 |
|---|---|---|---|
| **视频录制** | `monitor.py` + `detect.py` | `recordings/<主播>/<日期>/<主播>-<ts>-%03d.flv` | 否（仅拉流） |
| **弹幕/礼物采集** | `danmaku.py` + `danmaku_proto.py` | `<日期>/.meta/<主播>-<ts>.danmaku.jsonl` | **否，且必须否**（见 §3.2） |
| **字幕生成** | `subtitle.py` | 与视频同名同目录的 `.ass` + `.meta/*.align.json` | 否 |
| **归档** | `nas.py` | NAS `~/DouyinArchive/直播回放/<主播>/<日期>/` | 否 |

一句话概括链路：

```
检测开播 → ffmpeg 拉流分片录制 ─┬→ 视频 FLV
(每 300~1800s)                  └→ 弹幕/礼物 WS 长连接 → jsonl → .ass 字幕
                                                                    ↓
                                                        下播后整目录搬到 NAS
```

三块能力**相互隔离**：弹幕/礼物链路崩溃不影响录制，字幕生成失败不影响视频。这是设计上的硬要求（§3.2、§8）。

---

## 1. 全局数据流

```
┌─ detect.py ── 轮询主播是否开播（status=2 为直播中）
│     同时提取 stream_url（FLV 拉流地址）
│
├─ monitor.py::_start_recording
│     └─ subprocess.Popen(ffmpeg ...)         ← 产出分片 FLV
│
├─ monitor.py::_ensure_danmaku → danmaku.start_for
│     └─ WebSocket 长连接（匿名观众身份）      ← 产出 .meta/*.danmaku.jsonl
│
├─ monitor.py::subtitle_patrol_loop（每 60s）
│     └─ subtitle.patrol()                     ← 录制中增量生成 .ass
│
└─ monitor.py::_stop_recording → subtitle.finalize()
      ├─ 最后一次生成全部 .ass
      └─ align.json 写入 summary（弹幕数/礼物抖币/礼物榜）
             ↓
      nas.py 归档：整目录复制到 NAS（视频 + .ass + .meta 一起走）
```

**关键时序**：录制开始时写 `history.json`（`status=recording`）→ 结束回填 `ended_at/duration/size`（`status=done`）→ 归档状态机推进 `none → pending → archiving → archived`。

---

## 2. 视频录制

### 2.1 触发条件（三道闸，缺一不录）

| 闸 | 位置 | 说明 |
|---|---|---|
| ① 检测到开播 | `detect.py` | `status == 2` 且取到 `stream_url` |
| ② 该主播 `auto_record` 开关 | `config.json` → `monitors[].auto_record` | 关则不录**也不检测**（休眠态，省配额） |
| ③ `recorder.enabled` | `config.json` | 总开关 |

手动录制可绕过 ①：`POST /api/streamers/<name>/rec-start`。

### 2.2 ffmpeg 命令构造

代码：`monitor.py::_build_ffmpeg_args`（L518）。当前配置下的真实命令：

```bash
ffmpeg -y -loglevel error \
  -use_wallclock_as_timestamps 1 \     # ★关键：让 FLV tag 携带绝对毫秒墙钟
  -i <stream_url> \
  -copyts \                            # ★关键：分片不重置时间戳，跨分片连续
  -c copy \                            # 无 bitrate 配置时直接复制原流，几乎不占 CPU
  -f segment -segment_time 2300 -reset_timestamps 0 \
  "<录制约>/<主播>/<YYYY-MM-DD>/<主播>-<YYYYmmdd-HHMMSS>-%03d.flv"
```

- **`-use_wallclock_as_timestamps 1` + `-copyts` 是字幕对齐的地基**。弹幕是墙钟时间戳，视频是 FLV tag 时间戳，两者只在这两个参数下才落在同一坐标系。改动它们会让所有字幕错位。
- `bitrate` 非空时改为重编码：`-c:v libx264 -b:v <bitrate> -c:a aac -b:a 128k`（数字视为 Kbps，`5000` → `5000k`，也接受 `"3M"` 字符串）。**默认留空 = `-c copy`**，不要随意设。
- `segment_time = 0` 时退化为单文件输出（无 `-f segment`）。

ffmpeg 定位：`preflight.find_ffmpeg()` —— `which` → 兜底扫描 `/opt/homebrew/bin`、`/usr/local/bin`、`~/.homebrew/bin` 等。**找不到 ffmpeg 时只记日志不崩溃**，表现为"检测到开播但没文件"。

### 2.3 输出路径与命名

```
<输出根>/<主播名>/<YYYY-MM-DD>/<主播名>-<YYYYmmdd-HHMMSS>-%03d.flv
<输出根>/<主播名>/<YYYY-MM-DD>/<主播名>-<YYYYmmdd-HHMMSS>-000.flv   ← 实际分片
<输出根>/<主播名>/<YYYY-MM-DD>/<主播名>-<YYYYmmdd-HHMMSS>-000.ass   ← 字幕（同名同目录）
<输出根>/<主播名>/<YYYY-MM-DD>/.meta/<主播名>-<YYYYmmdd-HHMMSS>.danmaku.jsonl
<输出根>/<主播名>/<YYYY-MM-DD>/.meta/<主播名>-<YYYYmmdd-HHMMSS>.align.json
```

- `<输出根>` = `recorder.output_dir`，留空则为项目内 `recordings/`。
- **日期目录归属"开播日"**，不随跨天翻转。
- `.ass` 必须与视频同名同目录（播放器自动加载规则）；`jsonl`/`align` 收进 `.meta/` 隐藏目录，保持视频目录清爽。
- 路径推导函数：`danmaku.jsonl_for(output_path)`（兼容模板、单文件、实际分片三种输入形态）、`subtitle.align_path_for()`、`subtitle.ass_path_for_segment()`。

### 2.4 分片

- `segment_time = 2300`（约 38 分钟/片）。分片是 `-f segment` 的 `%03d` 序号，非独立 ffmpeg 进程。
- **每个分片独立锚定字幕**（见 §4.2），所以前面片段缺失/断链重连**不会向后传播错位**。

### 2.5 健康度防护（"假在线"问题）

代码：`monitor.py::_refresh_recording`（L853）。ffmpeg 在拉流断开时可能**既不退出也不再写数据**，形成空壳录制（历史出现过 17.82 小时空壳）。三道止损：

| 参数 | 默认 | 行为 |
|---|---|---|
| `stall_seconds` | 90 | 文件增长停滞超此时长 → 重启 ffmpeg |
| `stall_max_restarts` | 4 | 单场重启超此次数仍无数据 → **停止本场录制** |
| `stall_cooldown` | 600 | 停止后冷却 600s，避免流地址失效时空转刷历史 |

另：`_sync_recording_state` 暴露 `stalled` / `stall_seconds` 给前端告警；录制中的时长一律用**墙钟差值**（不用 ffmpeg 解析正在写入的文件，否则与历史页时长对不上），真实媒体时长仅在结束时用 `ffmpeg -i` 解析一次。

### 2.6 `history.json` 条目结构

文件：`recordings/history.json`（数组，最多保留最近 500 条）。真实样本：

```json
{
  "name": "主播A",
  "started_at": 1787339675.8220742,
  "output_path": ".../recordings/主播A/主播A-20260822-031435-%03d.flv",
  "status": "done",
  "ended_at": 1787340934.986558,
  "duration": 1259.2,
  "size": 1234567890,
  "archive": {
    "state": "archived",
    "target": "nas",
    "dest": "/Users/a/DouyinArchive/直播回放/主播A/2026-08-22",
    "at": 1788031987.0393531,
    "error": null
  }
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `name` | str | 主播名 |
| `started_at` / `ended_at` | float | Unix 秒（含小数） |
| `output_path` | str | **分片模板路径**（带 `%03d`），不是单个文件 |
| `status` | str | `recording`（进行中）/ `done`（已结束） |
| `duration` | float | 秒，优先 `ffmpeg -i` 解析的真实媒体时长，失败回退墙钟差 |
| `size` | int | 字节，部分旧条目无此字段 |
| `archive.state` | str | `none` 留本机 / `pending` 已入队 / `archiving` 搬运中 / `archived` 已归档 / `failed` / `missing` 文件缺失 |
| `archive.dest` | str \| null | NAS 落点目录（**归档副本的唯一索引，不可改**） |

> 当前 378 条：`archived` 288 / `none` 56 / `missing` 34。34 条 `missing` 是 8-20 前后的历史遗留，本机与 NAS 均无副本。

### 2.7 配置项（`config.json` → `recorder`）

| 键 | 当前值 | 说明 | 改动风险 |
|---|---|---|---|
| `enabled` | `true` | 总开关 | 🟡 关了全场不录 |
| `output_dir` | `""` | 留空=项目内 `recordings/` | 🟡 换目录后旧 `history.json` 路径失效，需跑 `migrate.py` |
| `format` | `"flv"` | 容器格式 | 🔴 改了字幕解析（FLV tag 读取）会失效 |
| `bitrate` | `""` | 留空=`-c copy` | 🟡 填了就重编码，CPU 暴涨 |
| `segment_time` | `2300` | 分片秒数 | 🟢 |
| `stall_seconds` | `90` | 停滞判定 | 🟡 太小会误重启 |
| `stall_max_restarts` | `4` | 重启上限 | 🟡 |
| `stall_cooldown` | `600` | 止损冷却 | 🟡 |

---

## 3. 弹幕与礼物采集

### 3.1 连接建立

端点：`wss://webcast100-ws-web-lq.douyin.com/webcast/im/push/v2/?...`（`danmaku._wss_url`，参数含 `room_id`、`user_unique_id`、`identity=audience` 等）

建连三要素：

| 要素 | 来源 |
|---|---|
| `signature` | `_sign()` 取关键参数 → `scripts/danmaku_sign.js`（`py_mini_racer` 执行） |
| `cookie: ttwid=...` | `_fetch_ttwid()` 匿名获取 |
| `User-Agent` | 固定 Windows Chrome 126 字符串（`danmaku.UA`） |

连接维持：

- **心跳**：`PushFrame(payload_type="hb")` 每 10 秒
- **ACK**：`resp.need_ack` 为真时回 `PushFrame(log_id, payload_type="ack", payload=resp.internal_ext)`
- **压缩**：`payload_encoding == "gzip"` 或首字节 `\x1f` → `gzip.decompress`
- **重试**：失败重试 3 次，退避 5/15/30 秒，之后**静默放弃**（不影响录制）

### 3.2 ⚠️ 硬约束：弹幕链路绝不能带登录态

抖音限制同账号同时只能进一个直播间。**带登录态连接弹幕会把正在录制的场次踢掉**。本项目靠匿名身份实现 4~5 路并发采集，登录态只用于开播检测（`detect.py`）且受 `cookie_guard` 限流保护。

> 对接时如需新增"带 Cookie 的连接"，必须确认它不会与在播录制争抢同一账号会话。

### 3.3 消息类型映射

| WS `method` | protobuf 类 | 写入 `type` | 备注 |
|---|---|---|---|
| `WebcastChatMessage` | `ChatMessage` | `chat` | 普通弹幕 |
| `WebcastGiftMessage` | `GiftMessage` | `gift` | 旧礼物通道 |
| `WebcastLightGiftMessage` | `LightGiftMessage` | `gift` | **新版主通道**，送礼人在 `common.user` |
| `WebcastMemberMessage` | `MemberMessage` | `member` | 需 `capture_member=true`（默认 false） |

解析失败**不静默丢弃**：`gift` 类失败计 `_gift_errors` 并打日志（礼物丢失排查的关键证据）。

### 3.4 jsonl 行格式

文件：`<日期>/.meta/<name>-<ts>.danmaku.jsonl`，每行一个 JSON。

```jsonl
{"ts": 1788716900625, "type": "chat", "user": "我***", "uid": "111111", "content": "对", "ts_local": 1788716911481}
{"ts": 1788716907893, "type": "gift", "user": "杨***", "uid": "111111", "gift": "小心心", "count": 2, "diamond": 100, "ts_local": 1788716911483}
{"ts": ..., "type": "member", "user": "某***", "uid": "...", "content": "进入直播间", "ts_local": ...}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `ts` | int | **服务器毫秒**（`common.create_time * 1000`，缺失时回落批次时间）。字幕对齐用这个值 |
| `ts_local` | int | **本地接收毫秒**（`_write` 时写入）。用于评估网络延迟与消息堆积 |
| `type` | str | `chat` / `gift` / `member` |
| `user` / `uid` | str | 昵称（部分打码）/ 用户 ID |
| `content` | str | 仅 chat / member |
| `gift` | str | 礼物名；解析不出时退化为 `礼物#<id>` 或 `?` |
| `count` | int | 数量，取 `repeat_count → combo_count → total_count → group_count → 1` |
| `diamond` | int | **单价抖币**（计算总额需 `diamond × count`） |

> 双时间戳是刻意设计：字幕按 `ts` 对齐画面，而"消息是否延迟到达"只能靠 `ts - ts_local` 判断。
>
> **弹幕与礼物共用同一个 jsonl**（靠 `type` 区分，礼物没有独立文件），两者时间戳同源
> （都是服务器 `common.create_time`），但礼物走的是另一条消息通道（`WebcastLightGiftMessage`
> 是连击聚合消息），实测与弹幕并不同步。所以对齐参数分两组：`offset_seconds` 管弹幕，
> `gift_offset_seconds` 在其之上再给礼物加一层（见 §4.2）。jsonl 本身**不做任何改写**——
> 偏移只在生成 .ass 时应用，原始事件永远可追溯。

### 3.5 礼物两条通道的差异

| | `WebcastGiftMessage` | `WebcastLightGiftMessage` |
|---|---|---|
| 送礼人 | `m.user` | `m.common.user` |
| 礼物名 | `m.gift.name` | `m.gift_struct.name`（退化： `礼物#<gift_info.gift_id>`） |
| 抖币 | `m.gift.diamond_count` | `gift_struct.diamond_count` → `gift_info.diamond_count` |
| 数量 | `repeat_count / combo_count / total_count` | `count / repeat_count / combo_count / group_count` |

两者都归一化成同一 jsonl 结构，下游无需区分。

**礼物数据目前没有独立 API**，消费方式只有两个：① 直接读 jsonl 里 `type == "gift"` 的行；② 读 `align.json` 的 `summary` 字段（场次级汇总）。

### 3.6 时钟漂移

`DanmakuRecorder.clock_drift_ms`：用响应里的 `resp.now` 与本地时间做**指数平滑**（0.9/0.1）。字幕生成时传入用于修正锚点（`drift_ms`），并写入 `align.json`。

### 3.7 诊断工具

```bash
# 抓礼物原始 payload 并盲解码(社区无公开 schema,需逆向)
.venv/bin/python collect_lightgift.py <room_id> [秒数=90]
#   样本原始 bytes 落 /tmp/lightgift_samples/

# 统计 WS 收到的所有 method + gift 解析成功率
.venv/bin/python diag_gift.py
```

### 3.8 配置项（`config.json` → `danmaku`）

| 键 | 当前值 | 说明 |
|---|---|---|
| `enabled` | `true` | 关则不采弹幕，字幕也不会生成 |
| `offset_seconds` | `0` | 弹幕全局偏移（秒），写入 `align.config_offset` |
| `gift_offset_seconds` | `0` | **礼物额外偏移**（秒），叠加在 `offset_seconds` 之上，写入 `align.gift_config_offset`；`0` = 礼物与弹幕同步 |
| `font_size` | `44` | ASS 字号 |
| `style` | `"queue"` | `queue` 底部队列 / `scroll` 横向滚动 |
| `queue_lines` | `5` | queue 模式行数（上限 8） |
| `queue_seconds` | `8` | 每条停留秒数 |
| `capture_member` | `false` | 是否记录进场消息（量大，默认关） |

---

## 4. 字幕生成

### 4.1 文件布局

```
<日期>/<name>-<ts>-000.flv          ← 视频
<日期>/<name>-<ts>-000.ass          ← 字幕（同名同目录，播放器自动加载）
<日期>/.meta/<name>-<ts>.danmaku.jsonl   ← 原始事件流（字幕的唯一数据源）
<日期>/.meta/<name>-<ts>.align.json      ← 锚点/偏移/异常/汇总
```

`.ass` **永远从 jsonl 无损重新生成**，可以反复调偏移而不损失原始事件。

### 4.2 对齐模型（核心，改动前务必读懂）

```
弹幕时间(秒) = (事件墙钟 ts − 该分片首包墙钟 anchor) / 1000 + offset
offset = config_offset(danmaku.offset_seconds)
       + global_offset(align.json，本场人工微调)
       + per_segment[idx](align.json，单分片微调)

礼物时间(秒) = (事件墙钟 ts − 该分片首包墙钟 anchor) / 1000 + offset + gift_extra
gift_extra = gift_config_offset(danmaku.gift_offset_seconds)
           + gift_global_offset(align.json，本场人工微调)
           + gift_per_segment[idx](align.json，单分片微调)
```

礼物那一层是**额外量**：`gift_extra = 0` 时礼物与弹幕完全同刻，改它不影响任何一条弹幕。
为什么要单独一格——礼物与弹幕不同通道，轻礼物是连击聚合消息（一次连击可能只推一条
汇总），时间戳常整体滞后；共用一组偏移时只能取折中，分开后各自可调。

**每个分片锚自己的起点**（`anchor_segments`），因此前面片段缺失或断链重连**不会向后传播错位**。

分片首包墙钟 `anchor` 的获取：

1. ffmpeg 加了 `-use_wallclock_as_timestamps 1 -copyts`，FLV tag 时间戳 = **epoch 毫秒截断到 32 位**（会回绕）
2. `flv_first_av_ts_abs()` 以文件 `birthtime` 为基准解回绕
3. 相对时间戳（`RELATIVE_TS_MAX = 86_400_000` 判定）或解析失败时，**回退 birthtime 本身**

> 所以 `-use_wallclock_as_timestamps` 一旦去掉，anchor 全部退化为 birthtime，字幕会整体漂移。

### 4.3 `align.json` 结构

```json
{
  "anchors": {"000": 1788716910094},
  "drift_ms": 377.8,
  "config_offset": 0.0,
  "global_offset": 0.0,
  "per_segment": {"000": 0.0},
  "gift_config_offset": 0.0,
  "gift_global_offset": 0.0,
  "gift_per_segment": {"000": 0.0},
  "gaps": [{"type": "stall", "from": 1788716910000, "to": 1788717000000, "note": "最后分片时间戳落后墙钟 45s"}],
  "finished_at": 1788717000000,
  "summary": {"chat": 66, "gift": 12, "diamonds": 3400,
              "gift_top": [{"gift": "小心心", "count": 8}]}
}
```

| 字段 | 说明 |
|---|---|
| `anchors` | 分片序号 → 首包墙钟毫秒 |
| `drift_ms` | 服务器-本地时钟漂移（指数平滑） |
| `config_offset` | 生成时快照的 `danmaku.offset_seconds` |
| `global_offset` / `per_segment` | 弹幕人工微调值（秒，前者范围 ±3600） |
| `gift_config_offset` | 生成时快照的 `danmaku.gift_offset_seconds` |
| `gift_global_offset` / `gift_per_segment` | 礼物人工微调值（**额外量**，叠加在弹幕偏移之上，±3600） |
| `gaps` | 异常区间：`stall`（视频停滞 >30s）、`danmaku_gap`（弹幕中断 >60s） |
| `summary` | 场次汇总（`subtitle.summarize()` 生成） |

### 4.4 生成时机

| 时机 | 函数 | 行为 |
|---|---|---|
| 录制中每 60s | `subtitle.patrol()` | 锚定新分片、检测停滞/缺口、**重生成全部 .ass** |
| 录制结束 | `subtitle.finalize()` | 最后生成一次 + 写 `summary` + `finished_at` |
| 人工调偏移 | `POST /api/danmaku/offset` | 从 jsonl 无损重建 |

### 4.5 样式参数

- **queue（默认）**：屏幕下方最多 `queue_lines` 行，新发言从底部挤入、旧行上移，每条停留 `queue_seconds` 秒，底部/顶部边距各 60px。
- **scroll**：`LANES = 12` 条轨道，滚过屏幕耗时 `SCROLL_SEC = 10` 秒。
- 礼物横幅停留 `GIFT_SEC = 5` 秒；播放分辨率固定 `1920×1080`；片尾保留 `GRACE_AFTER_END = 60` 秒。

### 4.6 场次汇总结构（`subtitle.summarize()`）

```json
{"chat": 66, "gift": 12, "diamonds": 3400,
 "gift_top": [{"gift": "小心心", "count": 8}]}
```

- `diamonds` = Σ (`diamond` × `count`)，**单位是抖币**
- `gift_top` 取数量 Top 10

### 4.7 人工微调接口

```
POST /api/danmaku/session   body {"path": "<输出路径>"}
  → {"segments":[{"idx","file","anchor","anchor_str",
                  "offset","extra","gift_offset","gift_extra","ass","ass_file"}],
     "global_offset","config_offset",
     "gift_global_offset","gift_config_offset",
     "gaps","drift_ms","has_events","summary"}

  segments[].offset      弹幕在该分片的生效偏移(含全局)
  segments[].extra       弹幕在该分片的**人工**值(面板输入框绑这个)
  segments[].gift_offset 礼物在该分片的生效总偏移(= offset + 礼物额外)
  segments[].gift_extra  礼物在该分片的**人工额外量**(面板输入框绑这个)

POST /api/danmaku/offset    body {"path":"...",
                                  "global_offset": 1.5, "per_segment": {...},
                                  "gift_global_offset": -0.5, "gift_per_segment": {...}}
  → 校验 ±3600 后写入 align.json 并重建 .ass
  两组键可独立提交:不带 gift_* 时礼物偏移保持原样;传 null(全局)或 {} (分片)表示清除
```

---

## 5. Web API 对接清单

基础：`http://127.0.0.1:8780`（端口见 `config.json → webui.port`，可用环境变量 `DOUYIN_MONITOR_PORT` 覆盖）。**本机请求务必加 `--noproxy '*'`**，否则可能被系统代理劫持。

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/status` | 全局状态（配置、在播、录制中） |
| GET | `/api/history` | 录制历史（含 archive 状态） |
| POST | `/api/history/delete` | 删除历史条目 |
| POST | `/api/streamers/<name>/rec-start` | 手动开始录制 |
| POST | `/api/streamers/<name>/rec-stop` | 手动停止录制（直播中也不再自动重录） |
| POST | `/api/streamers/<name>/refresh` | 立即检测该主播 |
| POST | `/api/danmaku/session` | 查某场次的分片锚点/偏移/汇总 |
| POST | `/api/danmaku/offset` | 调偏移并重建字幕 |
| GET/POST | `/api/nas` `/api/nas/test` `/api/nas/shares` | NAS 挂载与共享列表 |
| GET/POST | `/api/config` | 读写配置（**`monitors` 是整体替换语义**，只能增量 diff 后回写） |
| GET | `/api/service` `/api/service/control` | 服务状态与启停 |
| GET | `/api/preflight` | 环境自检（ffmpeg、依赖、NAS） |
| GET | `/api/retention` | 孤儿目录清理台账 |

> ⚠️ `POST /api/config` 传 `monitors` 会**整段覆盖**主播列表。历史上曾因此误删 9 位主播。对接时务必先 GET、在返回数组上做增量修改、再整体回写。

---

## 6. 文件与目录契约（外部程序解析用）

| 你需要 | 去哪拿 |
|---|---|
| 某场次的视频文件 | `history.json[].output_path` 是**模板**（含 `%03d`），用 `glob` 展开实际分片 |
| 某场次的字幕 | 把分片名后缀 `.flv` 换成 `.ass`（`subtitle.ass_path_for_segment`） |
| 某场次的弹幕/礼物流水 | `danmaku.jsonl_for(output_path)` → `.meta/*.danmaku.jsonl` |
| 该场次是否归档、落在哪 | `history.json[].archive.state` / `.dest` |
| 场次级礼物统计 | `.meta/*.align.json` → `summary` |
| 归档后的文件 | NAS `<mount>/<root_dir>/<主播>/<日期>/`，视频与 `.ass`、`.meta` **整目录一起搬** |

命名约定：`<主播名>-<YYYYmmdd-HHMMSS>` 是同一场次的前缀，视频分片、字幕、jsonl、align **共享这个前缀**，靠目录与后缀区分。

---

## 7. 对接时的安全边界

### 🔴 禁止改动

| 项 | 原因 |
|---|---|
| ffmpeg 的 `-use_wallclock_as_timestamps 1` + `-copyts` | 字幕对齐的地基，去掉后全量漂移 |
| 弹幕/礼物链路使用匿名身份 | 带登录态会踢掉在播录制（§3.2） |
| `recordings/history.json` 已 `archived` 条目的 `dest` | NAS 副本的唯一索引 |
| `BASE` 路径无关约定 | 整体搬家靠它 + `migrate.py`，写死绝对路径会让迁移失效 |
| `_atomic_write_json` 原子写语义 | 直接覆盖写会在崩溃时留下半截 JSON |

### 🟡 需要谨慎

- 改 `recorder.format`：字幕模块按 FLV tag 解析时间戳，换容器需同步改 `subtitle.py` 的解析。
- 改 `segment_time`：影响分片数量与锚点数量，已有 `.ass` 需重新生成。
- 改 `output_dir`：旧 `history.json` 里的绝对路径会失效，需跑 `migrate.py --from <旧根> --apply`。
- 新增 WS 消息类型：`danmaku._dispatch` 里加分支，解析失败要计数而不是静默丢弃。

### 🟢 可自由调整

`danmaku.font_size` / `style` / `queue_lines` / `queue_seconds`（改完重建 `.ass` 即可）、`stall_*` 系列参数、`ass_backup` 之外的清理策略。

---

## 8. 常见误解与排查

| 现象 | 真实原因 |
|---|---|
| "检测到开播但没有文件" | ffmpeg 未找到（只记日志不崩溃），或 `recorder.enabled=false`，或该主播 `auto_record=false`（休眠态，连检测都不做） |
| 字幕整体偏早/偏晚 | 时钟漂移或对端延迟，用 `/api/danmaku/offset` 调 `global_offset`（±3600s 内） |
| 礼物横幅与弹幕对不齐（弹幕准、礼物偏） | 两者不同通道，礼物是连击聚合消息。长期用 `danmaku.gift_offset_seconds` 设默认，单场用 `/api/danmaku/offset` 传 `gift_global_offset`（只动礼物，弹幕不受影响） |
| 某分片字幕错位 | 该分片 anchor 退化成了 birthtime（FLV tag 解析失败），看 `align.json.anchors` |
| 礼物数比直播间看到的少 | 只采 WS 推送；`WebcastLightGiftMessage` 解析失败会打 `⚠️ GiftMessage 解析失败` 日志，用 `diag_gift.py` 查 method 分布 |
| `duration` 与实际不符 | 优先取 `ffmpeg -i` 真实媒体时长；分片缺失/解析失败才回退墙钟差 |
| 请求返回 000/超时 | 端口在 LISTEN 但服务僵死，或 curl 走了系统代理 —— 用 `curl --noproxy '*'`，仍不行看 `logs/launchd.err.log` |
| 归档后本机文件没了 | 正常：归档按日期目录整目录复制到 NAS，校验大小后才删源 |
| 全员 `All connection attempts failed`，历史记录停止增长 | 服务继承了**已失效的环境代理**（`http_proxy` 指向死端口）。`monitor.py` 启动时会清掉并记 `[自检] 已清除 N 个环境代理变量`；若仍异常，查 `ps eww -o command -p <pid> \| grep -i proxy`。见框架说明书 §7 |

---

## 9. 验证清单（对接后自检）

```bash
cd /Users/a/Downloads/douyin-monitor

# 1) 代码与文档一致
.venv/bin/python scripts/check_docs_sync.py

# 2) 全套单元测试（293 项）
.venv/bin/python -m unittest discover -s tests

# 3) 服务与 NAS
curl -s --noproxy '*' 127.0.0.1:8780/api/service   # running=true
curl -s --noproxy '*' 127.0.0.1:8780/api/nas       # mounted=true, backoff_s=0

# 4) 抽查最近一场的四件套是否齐全(视频 / .ass / jsonl / align)
.venv/bin/python - <<'EOF'
import json, os, subtitle
h = json.load(open('recordings/history.json'))
# 自动挑一个「本机仍有分片文件」的场次(已归档的源目录会被删掉)
cand = [x for x in h if subtitle.find_segments(x['output_path'])]
print('本机仍有分片的场次:', len(cand))
e = cand[-1]; out = e['output_path']
segs = subtitle.find_segments(out)
print('主播:', e['name'], '| 归档:', (e.get('archive') or {}).get('state'))
print('分片:', [os.path.basename(s[1]) for s in segs][:3])
print('字幕存在:', [os.path.exists(subtitle.ass_path_for_segment(s[1])) for s in segs][:3])
j = subtitle.jsonl_for(out)
print('jsonl:', os.path.basename(j), os.path.exists(j))
print('汇总:', subtitle.load_align(out).get('summary'))  # 录制中(未 finalize)时为 None
EOF
```

实测参考输出（2026-09-08，本机 52 场待归档）：

```
分片: ['主播A-20260907-023536-000.flv', '-001.flv', '-002.flv']
字幕存在: [True, True, True]
jsonl: 主播A-20260907-023536.danmaku.jsonl True
汇总: None        ← 该场次 finalize 时才会写入
```

> 改动代码后请同步更新 `项目框架说明书.md` 与本文档，并跑 `scripts/check_docs_sync.py`（约定见框架说明书 §9）。
