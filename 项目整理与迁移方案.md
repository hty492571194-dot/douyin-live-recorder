# 项目整理与迁移方案

> 生成时间：2026-09-06 · **状态：方案 B 已于 2026-09-07 执行完成**
> 当前项目根：`/Users/a/Downloads/douyin-monitor`（原文中的 `/Users/a/Downloads/douyin-monitor-phase-d-19-g3fe4950` 仅为迁移前的旧路径，保留作历史记录）
> 一~三章是当时的扫描与决策记录，**实际执行结果与后续回退方式见文末「五、执行结果」**。

---

## 一、现状快照

| 位置 | 占用 | 性质 |
|---|---|---|
| `recordings_backup_20260825/` | 84G | 历史备份 |
| `recordings/` | 71G（flv 69.2G + ass 2.5M） | 运行时数据 |
| `auth/profile/` | 391M | Chrome 登录 profile |
| `.venv/` | 264M | 依赖目录 |
| `previews/` | 2.9M | 运行时数据 |
| `logs/` | 988K | 日志 |
| `hotspots.json` / `hotspot_events.json` / `config.json` | ~22K | 配置与状态 |
| NAS `~/DouyinArchive/直播回放` | 407G | 归档总库（项目外） |

**关键结构性观察**：源码（16 个 .py）、配置、运行时数据、大媒体文件、历史备份**全部平铺在项目根**，没有 `app/` `data/` 分层。这既让"哪些能删"变难判断，也让"整盘拷走"要么全拷 155G、要么容易漏拷 `hotspots.json` 这类小但关键的状态文件。

---

## 二、可安全清理 / 需保留清单

### 2.1 可清理（按风险从低到高）

| 项目 | 占用 | 判断依据 | 可自动重新生成 | 删除风险 |
|---|---|---|---|---|
| `__pycache__/`（根目录 + `tests/`） | 472K | Python 字节码缓存，`.gitignore` 已明确忽略 | ✅ 运行即生成 | 无 |
| 各目录 `.DS_Store` | 10K+ | macOS Finder 元数据，非项目资产 | ✅ Finder 自动重建 | 无 |
| `config.json.default.bak` | 1K | 8-20 的默认配置模板，内容与 `config.example.json` 重复 | ✅ 等同已有文件 | 无 |
| `spool/pending.json.bak-151754` | 2B | 归档队列备份；当前 `pending.json` 为空（`[]`），队列已清空 | ✅ 运行时生成 | 极低 |
| `align-bak-20260901.tar.gz` | 64K | 9-1 音画对齐操作的备份包，对齐早已完成并归档 | ❌ 但已无用途 | 低 |
| `logs/monitor.out.log` | 7K | 8-20 早期遗留，与 `launchd.out.log` 重复 | ❌ | 低 |
| `logs/monitor-2026-08-30/31.log` 等 7 天前日志 | ~120K | 日志按天轮转、无自动清理；8 月日志仅用于追溯旧问题 | ❌ | 低（建议保留最近 7 天，其余归档后删） |
| `ass_backup_20260825/` | 2.6M | 8-25 弹幕字幕备份；NAS 归档中已有 632 个 `.ass` | ❌ | 低-中：删除前建议抽查 2-3 位主播在 NAS 的 `2026-08-2x` 目录是否齐全 |
| `auth/profile/` | 391M | 扫码登录用的 Chrome profile | ✅ 重新扫码即生成 | 中：删后下次需重新扫码登录；**钥匙串里的 Cookie 不受影响** |
| `recordings_backup_20260825/` | **84G** | 8-25 的录制整包备份 | ❌ | **高**：必须先核对 NAS `直播回放/<主播>/2026-08-2x` 与本机一一对应后才能删。这是本机最大的一块 |
| NAS `_bak_trash_20260901` | 待测 | 9-1 标记的待删目录 | ❌ | 中：需你确认内容后再处理 |
| 孤儿主播目录：`recordings/主播A`、`recordings/主播B`、`previews/主播C`、`previews/主播B` | 待测 | 这 3 位已不在主播列表，但历史文件仍在 | ❌ | 中：可能含未归档素材，删前需在「录制历史」页确认该主播条目都已 `archived` |
| `.venv/` | 264M | 依赖目录 | ✅ `pip install -r requirements.txt` | 中：删了服务立刻起不来；且**当前 `requirements.txt` 不完整**（只列 4 个，实际还装了 `playwright`、`imageio-ffmpeg`、`httpx` 生态），重建前必须先补全 |
| 12 个孤儿 ffmpeg 进程 | — | ppid=1、CPU 0%、已跑 20+ 小时（进程非文件） | — | 低：建议清理，但不影响文件内容 |

### 2.2 关于 `recordings/`（71G）的特别说明 —— **不建议整目录删**

按天分布：`08-31 0.6G / 09-01 6.3G / 09-02 10.5G / 09-03 6.3G / 09-04 17.6G / 09-05 12.3G / 09-06 15.6G`

`history.json` 355 条的归档状态：`archived 281 / missing 29 / none 46`。**`none` 的 46 条是最近几场（含当天正在录的那一场），尚未归档**，删了就是真丢。

安全做法（不是删除目录，而是逐条判断）：
1. 在「录制历史」页筛选「未归档」，先看这 46 条是不是还想留；
2. 已 `archived` 的条目，项目归档时会把文件移走，本机残留的多是归档失败或跳过的；
3. 需要我按"已归档 + NAS 上确有其文件"生成一份精确到路径的待删清单，我可以做（只出清单，不删）。

### 2.3 必须保留

| 项目 | 理由 |
|---|---|
| `*.py`（monitor/webui/auth/detect/nas/danmaku/preview/schedule/preflight/cookie_guard 等 16 个） | 源码，无可替代 |
| `static/index.html` | 全部前端 UI |
| `config.json` | 主播列表、调度、录制、NAS 全部配置；**丢失=主播列表没了**（今天刚吃过这个亏） |
| `config.prev.json` | 写前上一版（护栏，服务重启后开始产生） |
| `recordings/history.json` | 355 条录制索引，含归档去向；丢了历史页全空 |
| `hotspots.json` / `hotspot_events.json` | 开播热点模型与开播时刻样本，靠长期累积，删了要重新训练 |
| `auth/storage_state.json` / `cdp.json` / `meta.json` | 登录态与 CDP 端口记录（Cookie 主体在钥匙串，但状态文件丢了要重新登录） |
| `previews/<10 位主播>/` | 主播卡片头像 |
| `logs/` 最近 7 天 | 排查在播/录制问题的唯一线索 |
| `tests/`（21 个测试文件，380 项） | 回归保障 |
| `scripts/`（含 `danmaku_sign.js` 485K、pack/verify/backfill 脚本、`ctl.py` 终端指令、`install_cli.sh` 安装脚本） | 弹幕签名与运维脚本 |
| `requirements.txt` | 依赖声明（**需补全**） |
| 文档：`README.md` `PROJECT_GUIDE.md` `AI_AGENT_GUIDE.md` `AI开发任务书.md` `开发项目书.md` `agents/` | 项目知识与契约 |
| **项目外**：`~/Library/LaunchAgents/com.douyin.monitor.plist`、`~/.local/bin/douyin`（由 `scripts/install_cli.sh` 生成，搬家后重跑安装即修复）、钥匙串 `douyin-cookie`/`douyin-nas`、NAS `~/DouyinArchive/直播回放` | 服务定义、快捷指令入口、凭据、归档总库 |

---

## 三、整合到统一父文件夹：可行性分析

### 3.1 好消息：代码本身是可移植的

所有模块都用 `BASE = os.path.dirname(os.path.abspath(__file__))` 定位（monitor/webui/nas/auth/detect/preview/danmaku + 3 个 scripts），**没有硬编码 `/Users/a/...` 路径**；shell 脚本（`setup.sh`、`scripts/*.sh`、`一键启动.command`）里也没有绝对路径。这意味着：**整个目录搬到任何路径/任何 Mac，代码逻辑都能正确找到自己的家**。

### 3.2 需要迁移 / 会在迁移中失效的内容

| # | 项目 | 现状 | 迁移后影响 | 处理 |
|---|---|---|---|---|
| 1 | `.venv/` | `pyvenv.cfg` 写死 `home = /Users/a/.workbuddy/binaries/python/3.13.12/bin`（WorkBuddy 托管 Python） | **换机后 venv 直接失效**：新机器没有这个托管 Python | 迁移后删除重建 venv；`requirements.txt` 需先补全 |
| 2 | `history.json` | **355 条全部存绝对路径**，281 条 `archive.dest` 也是绝对路径 | 换机/换目录后历史页全部显示"文件缺失" | 项目已有「重新定位」逻辑（日志中 `[历史] 重新定位`），可自愈一部分；更彻底的做法是改存相对路径 |
| 3 | `com.douyin.monitor.plist` | `ProgramArguments` 与 `WorkingDirectory` 写死当前绝对路径 | 换机/换目录后服务起不来 | 迁移后重新生成 plist（用新路径）并 `launchctl bootstrap` |
| 4 | 钥匙串 | Cookie 与 NAS 密码存在系统钥匙串，**不随文件夹走** | 新机器需重新扫码登录 + 重填 NAS 密码 |  unavoidable，写进迁移检查清单 |
| 5 | `auth/profile/` | Chrome profile 绑定本地路径 | 新机器重新扫码即可 | 可以不拷（省 391M） |
| 6 | NAS 挂载 | `mount_point = ~/DouyinArchive`（含 `~`，可移植）| 新机器需自行挂载 SMB 并保证同名挂载点 | 文档说明 |
| 7 | 目录名 | `douyin-monitor-phase-d-19-g3fe4950` 带 git 版本号后缀 | 不好看、易混淆，但不影响运行 | 建议改名 `douyin-monitor` |
| 8 | 孤儿 ffmpeg | 进程级，不随文件迁移 | 新机器无此问题 | 迁移前清掉即可 |

### 3.3 三个方案对比

**方案 A · 大重构：拆成 `app/ data/ media/ logs/` 四层**
- 做法：`mkdir app data media logs`；源码进 `app/`，配置与状态进 `data/`，`recordings/ previews/` 进 `media/`，日志进 `logs/`
- 收益：职责清晰，"能删什么"一眼可辨；备份时可只打包 `app/ + data/`（几十 MB）
- 成本：**所有 `BASE` 语义要改（现在 `BASE` 既是代码根也是数据根）**，涉及 monitor/webui/nas/auth/preview/danmaku 6 个模块 + 3 个脚本；`_allowed_roots`、录制输出路径、预览目录全部要跟着改；71G 媒体要搬（同盘移动快，跨盘慢）
- 风险：中-高。252 项测试能兜住大部分，但路径类改动容易在某个角落漏网

**方案 B · 改名 + 可移植化改造（不移动文件）** ⭐ 推荐
- 做法：
  1. 目录改名 `douyin-monitor-phase-d-19-g3fe4950` → `douyin-monitor`（固定名，不再带版本号）
  2. 补全 `requirements.txt`（加 `playwright`、`imageio-ffmpeg` 等实际用到的）
  3. 新增 `setup.sh` 一键装机：建 venv → 装依赖 → 按当前路径生成 plist → `launchctl bootstrap`
  4. 新增 `migrate.py`：把 `history.json` 里的旧绝对路径前缀批量重写为新前缀（支持 `--dry-run`）
  5. 补一份「换机检查清单」（扫码登录、NAS 密码、挂载点）
- 收益：直接命中"拷到其他电脑可直接运行"的目标；改动集中在新增脚本，不动任何运行时路径逻辑
- 成本：低。测试几乎不受影响
- 风险：低

**方案 C · 什么都不改，写一份迁移手册**
- 收益：零风险
- 成本：每次换机手工 5-6 步，容易漏（尤其 plist 与 venv）
- 风险：低，但目标达成度也低

### 3.4 推荐：先 B，再视情况做 A

**先做 B** 的理由：A 的收益是"整洁"，B 的收益是"能跑"，而你的目标写的是"拷贝到其他电脑后可直接运行"——B 用 1/10 的风险达成 90% 的目标。A 里最有价值的两件事（备份只打包代码+配置、一眼看出能删什么）可以靠 B 阶段的 `requirements.txt` 补全 + 本文这份清单先行获得。

**什么时候再做 A**：等项目稳定、不再频繁改路径类逻辑时，用 `git mv` 分批做（建议顺序：`logs/` → `previews/` → `recordings/` → 状态 json → 源码），每一步跑一次 252 项测试 + Playwright 冒烟。

### 3.5 若执行方案 B，验证步骤

> ✅ 以下 7 步已于 2026-09-07 实际执行并通过，详见文末「五、执行结果」。

1. `cp -R` 整个目录到 `/tmp/douyin-monitor-test/`（不含 `.venv`、`auth/profile`）
2. 在新目录 `python3 -m venv .venv && pip install -r requirements.txt`
3. `python migrate.py --from /Users/a/Downloads/douyin-monitor-phase-d-19-g3fe4950 --to /tmp/douyin-monitor-test --dry-run` 看 history 路径重写条数
4. `./setup.sh` 生成 plist（指向新路径），`launchctl bootstrap` 到**临时 Label**（如 `com.douyin.monitor.test`）避免干扰现网服务
5. 访问临时端口，检查：主播列表 10 位、录制历史 355 条、归档路径可打开
6. 单元测试：`python -m unittest discover -s tests` 252 项全过
7. 验证通过后 `launchctl bootout` 临时服务，正式切换

---

## 四、假设与待确认

1. **假设** `recordings_backup_20260825/` 是 8-25 一次性备份而非增量备份链的一环（依据：目录名带日期、之后无同名新目录）。若它是唯一副本，则**不可删**。
2. **假设** NAS `~/DouyinArchive/直播回放` 是归档唯一正本（依据：`archive.mode=auto`、281 条 `archived` 指向此处）。删除本机任何录制前都应以它为基准核对。
3. **待确认** NAS 上 `_bak_trash_20260901` 的内容与体积（SMB 上 `du` 很慢，未执行）。
4. **待确认** `recordings/` 里 8-31 至 9-03 的旧文件为何未随归档移走（归档失败 or 被跳过），这决定它们能否删。
5. **假设** 目标是"同一台机器换目录"或"换一台 Mac"，不涉及跨平台（Windows/Linux）—— 若涉及，`open` 命令、launchd、钥匙串三处都要改。

---

## 五、执行结果（2026-09-07 完成）

### 5.1 实际做的事

| # | 动作 | 结果 |
|---|---|---|
| 1 | 备份 `config.json` / `history.json` 到 `/tmp/pre-migrate-*.json` | ✅ |
| 2 | `launchctl bootout` 停服 | ✅（4 场在播录制中断，已确认接受） |
| 3 | 目录改名 → `/Users/a/Downloads/douyin-monitor` | ✅ |
| 4 | 验证新路径下 venv 可导入 httpx / websocket / betterproto / playwright | ✅ |
| 5 | `migrate.py --from <旧根> --apply` 重写历史与配置中的绝对路径 | ✅ 旧路径残留 0 条 |
| 6 | `setup.sh --reinstall-plist` 用新路径重新生成 plist | ✅ plist 内容正确 |
| 7 | 启动服务并验证：10 位主播加载、录制落盘新路径、单元测试全过 | ✅ |

### 5.2 迁移后暴露的四个问题（均已修复）

| 症状 | 根因 | 修复 |
|---|---|---|
| 前端显示「服务已停止」，但 UI 能正常用 | `_service_status()` 只查 launchd；迁移时被 `bootout` 过，之后手动拉起的进程不在 launchd 名下 | launchd 查不到时回退按 Web 端口找进程，新增 `source=manual`，前端显示「服务运行中(未托管)」 |
| NAS 提示挂载失败 | 配置里 `nas.share` 被改成了带 `_公共空间` 后缀的共享名，实际要挂载的是不带后缀的那个（同一台 NAS 上共 4 个共享，带后缀的是另一条路径） | 改回正确共享名，备份 `config.json.bak-sharefix` |
| 「测试连接」报 `已挂载但写入失败: [safe-delete] ... FSMoveObjectToTrashSync failed` | `test_connection()` 里探针写完后 `os.remove`，删除失败（SMB 回收站/占用/安全软件）被当成不可写 | 探针改为**只写+读回、不删除**，删除失败一律忽略。同理的 `ensure_mount` 分支此前已改 |
| 点「重启」毫无反应，服务跑的一直是旧代码（2026-09-11 才定位） | 手动拉起的孤儿进程（PPID=1，不在 launchd 名下）长期占用 8780；launchd 每次 spawn 都 `Address already in use` 退出、每 10 秒重试（累计 4598 次、err.log 7.5MB），而 `kickstart -k` 只作用于 launchd 名下进程，对那个孤儿完全无效——界面连的始终是它 | 结束该进程交还 launchd 托管（`state = running`、`source=launchd`）；代码侧 `_outsider_pid()` 识别这类外来户，重启时先 `SIGTERM` 再 `kickstart`；`start` 改为 `bootout`→`bootstrap`；plist 与 `setup.sh` 同步加 `ThrottleInterval=60` / `ExitTimeOut=30` |

> ⚠️ 教训：`ensure_mount` 一旦判定"不可写"就会 `umount -f` 强行卸载。让一个 5 字节探针的删除失败引发卸载**正在归档的 NAS**，代价完全不对等——判定只能以「写入成功」为准。

### 5.3 回退方式

```bash
# ① 停服
launchctl bootout gui/$UID/com.douyin.monitor 2>/dev/null; pkill -f "monitor.py"

# ② 目录改名回去
mv /Users/a/Downloads/douyin-monitor /Users/a/Downloads/douyin-monitor-phase-d-19-g3fe4950

# ③ 把路径反向重写回旧前缀
cd /Users/a/Downloads/douyin-monitor-phase-d-19-g3fe4950
.venv/bin/python migrate.py --from /Users/a/Downloads/douyin-monitor \
                            --to /Users/a/Downloads/douyin-monitor-phase-d-19-g3fe4950 --apply

# ④ 重新生成 plist 并装载
./setup.sh --reinstall-plist
```

### 5.4 下次再迁移 / 换机的标准流程

```bash
# 1) 备份两份关键 json
cp config.json /tmp/config.bak.json && cp recordings/history.json /tmp/history.bak.json

# 2) 搬目录（同盘 mv 是秒级的）
mv <旧根> <新根> && cd <新根>

# 3) 重写绝对路径（先 dry-run 看条数，再 --apply）
.venv/bin/python migrate.py --from <旧根> --dry-run
.venv/bin/python migrate.py --from <旧根> --apply

# 4) venv 里的绝对路径也要重建（venv 不可直接搬，跨机尤其如此）
rm -rf .venv && ./setup.sh

# 5) 重新生成 plist（含新 WorkingDirectory）
./setup.sh --reinstall-plist

# 6) 重装终端快捷指令（把新项目根写进 ~/.local/bin/douyin；幂等，可重复跑）
bash scripts/install_cli.sh

# 7) 验证
.venv/bin/python -m unittest discover -s tests      # 全过
.venv/bin/python scripts/check_docs_sync.py         # 文档未漂移
douyin status                                       # 服务 + 健康 + 直播一眼看完
curl -s --noproxy '*' 127.0.0.1:8780/api/service    # running=true
curl -s --noproxy '*' 127.0.0.1:8780/api/nas        # mounted=true
curl -s --noproxy '*' 127.0.0.1:8780/api/shortcuts  # installed=true（入口装好了）
```

**为什么要单独有第 6 步**：终端快捷指令的入口 `~/.local/bin/douyin` 在**项目外**，里面记着项目绝对路径。搬完家不重跑安装脚本，`douyin` 就会找不到项目（或指向旧目录）。安装脚本是幂等的，重跑一次即修复，`--uninstall` 可整体卸掉。

**兜底**：即便漏了第 6 步，双击 `一键启动.command` 也会检测到入口缺失并自动补装（同样幂等）。Web 控制台「系统健康」页的「终端快捷指令」卡片还会直接标出入口装了没（`/api/shortcuts` 的 `installed` 字段），没装时把修复命令写在卡片上。

**命令清单的唯一真源**：`scripts/ctl.py` 顶部的 `COMMANDS` / `ZSH_ALIASES` 两张表（对外出口 `cheatsheet()`）。argparse 帮助、`douyin help`（`--short` / `--json`）、双击 `一键启动.command` 时窗口里打印的速查表、Web「系统健康 → 终端快捷指令」卡片（`GET /api/shortcuts`）**都由它渲染** —— 所以搬家/改命令后不需要去改启动脚本或前端，它们显示的内容自动就是最新的（`tests/test_ctl.py::TestCheatsheet` 与 `tests/test_shortcuts_api.py` 守着这条）。

**跨机额外三步**：① 扫码登录重建（`auth/` + 钥匙串 `douyin-cookie`）；② NAS 密码存回钥匙串 `douyin-nas`；③ 确认 `nas.mount_point` 在新机器上存在。

### 5.5 文档同步约定（2026-09-07 起生效）

**任何代码/配置改动后，必须同步更新根目录文档，否则文档会与代码脱节。** 受影响的文件：

| 文件 | 什么时候必须更新 |
|---|---|
| `项目框架说明书.md` | 模块增删、API 增删、配置项增删、数据流变化、安全边际变化 |
| `项目整理与迁移方案.md` | 目录结构变化、路径变化、保留/清理清单变化、换机流程步骤变化 |
| `AI_AGENT_GUIDE.md` | 项目根路径、端口、模块清单、启动方式变化 |
| `README.md` / `PROJECT_GUIDE.md` | 依赖、启动方式、目录结构、快捷指令变化 |

另外三条纪律：改服务启停规则只改 `webui._service_plan()`（Web 按钮与终端 `douyin` 共用，各写一份必然分叉）；增删命令只改 `scripts/ctl.py` 的 `COMMANDS` / `ZSH_ALIASES` 表（argparse 帮助、`douyin help`、`一键启动.command` 窗口、Web「系统健康」页的快捷指令卡片——数据 `GET /api/shortcuts`——都由它渲染，别处不许另抄一份）；`~/.local/bin/douyin` 是 `scripts/install_cli.sh` 生成的产物，不要手改那个文件。

自动化兜底：`scripts/check_docs_sync.py`（已纳入 `tests/test_docs_sync.py`）会校验文档中的项目根路径、端口、模块清单、以及说明书里写到的 `/api/xxx` 是否真实存在。**改完代码跑一次全套测试即可发现文档漂移。**
