# 抖音直播监控 · 项目框架说明书

> 版本：v1.0（2026-09-07）
> 项目根：`/Users/a/Downloads/douyin-monitor`
> 面向读者：接手本项目的另一个人 / 另一个智能体。
> 阅读建议：**只想要改一个功能 → 直接看 §1 速查表**；要动手改核心链路 → 必读 §6 安全边际。

---

## 0. 一句话概览

一个 macOS 上长期运行的**抖音直播自动录制系统**：定时轮询主播是否开播 → 开播则拉流录制为分片 FLV → 同时抓弹幕 → 主播下播后把成片搬到 NAS 归档 → 弹幕按分片时间轴生成 .ass 字幕。附带一个本地 Web UI（默认 `http://127.0.0.1:8780`）做配置、查看与人工干预。

技术栈：Python 3.13 + asyncio + httpx + ffmpeg + Playwright（仅登录用）+ 标准库 `http.server`（Web UI，无框架）。
持久化：全部为 **JSON 文件**，无数据库。秘钥（抖音 Cookie / NAS 密码）存 **macOS 钥匙串**。

---

## 1. 改动定位速查表（Token 最省入口）

| 你想改什么 | 去哪个文件 | 关键位置 |
|---|---|---|
| 主播列表 / 自动录制 / 备注字段 | `config.json` → `monitors[]` | 每项是 `{name, anchor, web_rid, douyin_id, preview, added_at, auto_record, note}` |
| 前端页面（主播卡、备注弹窗、指引页、健康面板） | `static/index.html`（单文件，含 CSS/JS） | 视图 section：`#view-streamers` / `#view-params` / `#view-advanced` / `#view-guide` / `#view-health` / `#view-nas` / `#view-history` |
| **高级参数页有哪些字段 / 怎么分组折叠** | `static/index.html` 的 `ADVANCED_GROUPS` 表 | 本卡字段唯一真源(25 项 / 5 组):改标签、加字段、挪分组只动这张表;折叠只切 class 不摘 DOM,收起组的字段照旧保存 |
| 新增一个 Web API | `webui.py` | `make_handler()` 内的 `do_GET`(L989) / `do_POST`(L1078) 分支 |
| 检测是否开播 | `detect.py` | 302 探测 + room status 双通道 |
| 检测频率 / 热点窗口 | `schedule.py` + `monitor.py` 的 `run_streamer`(L1133) | 冷/热/在播三档间隔 |
| Cookie 使用额度限制 | `cookie_guard.py` | 三道闸门（场景/频率/配额） |
| 录制参数（分片、码率、卡死重启） | `monitor.py` | `_build_ffmpeg_args`(L518) / `_recording_watchdog`(L937) |
| 弹幕抓取 | `danmaku.py` + `danmaku_proto.py` | WebSocket 匿名连接，签名用 `scripts/danmaku_sign.js` |
| 弹幕字幕 / 时间对齐 | `subtitle.py` | 分片独立锚定 + `align.json` 微调 |
| NAS 挂载与归档 | `nas.py` | `ensure_mount` / `enqueue` / `process_queue` / `nas_watch` |
| 录制历史与归档状态机 | `monitor.py` | `_history_add`(L423) / `_mark_archive_state`(L463)，数据 `recordings/history.json` |
| 头像预览图 | `preview.py` | `previews/<name>/` |
| **孤儿目录 30 天延迟清理** | `retention.py` | `mark` / `sweep` / `run_due`，台账 `spool/retention.json` |
| 启动自检项 | `preflight.py` | `run_checks(config)` |
| 登录态（扫码 / 钥匙串） | `auth.py` | 只服务检测通道，**不参与弹幕** |
| 项目搬家改路径 | `migrate.py` | `--from OLD_ROOT --apply` |
| 服务注册 / 依赖安装 | `setup.sh` + `~/Library/LaunchAgents/com.douyin.monitor.plist` | `--reinstall-plist` |
| **服务启停的规则（先清端口外来实例等）** | `webui._service_plan()` | 唯一定义处；Web 按钮与终端 `douyin` 共用，改这里即两边生效 |
| **终端快捷指令（dy / dyr / dyon / dyoff）** | `scripts/ctl.py` ← `~/.local/bin/douyin` | 装/修：`bash scripts/install_cli.sh`；命令速查 `douyin help` |
| **有哪些命令、各自什么效果** | `scripts/ctl.py` 的 `COMMANDS` / `ZSH_ALIASES` 表（出口：`cheatsheet()`） | 唯一真源：argparse 子命令、`douyin help`（`--short` / `--json`）、`一键启动.command` 窗口、Web「系统健康 → 终端快捷指令」卡片，全部由此渲染 |
| **Web 上哪里能看到这份命令清单** | 「系统健康」页 → 「终端快捷指令」卡片 | 数据 `GET /api/shortcuts`（后端 `webui._shortcut_help()` 现取 `ctl.cheatsheet()`）；前端只排版、不硬编码命令名 |
| **各组件是否卡住（系统健康面板）** | `health.py` + `webui._health_overrides()` | 打点 `health.beat(key)`；接口 `GET /api/health`；前端「系统健康」页 |
| **把本地改动同步到 GitHub（月度）** | `scripts/sync_github.py` | 唯一入口：扫描敏感内容 → 提交 → 走 Git Data API 推送。禁词集从 `config.json` 反推，**不要硬编码主播名/内网地址** |

---

## 2. 目录结构

```
/Users/a/Downloads/douyin-monitor/
├── monitor.py            (1618 行) 主程序：调度中枢、录制进程、历史状态机
├── webui.py              (1782 行) 内嵌 Web 服务（标准库 http.server）
├── detect.py              (341)    双通道开播检测
├── schedule.py            (305)    热点窗口 + 开播事件 + 自适应轮询
├── cookie_guard.py        (171)    Cookie 风控护栏
├── auth.py               (1169)    登录态管理（扫码/钥匙串/巡检）
├── danmaku.py             (406)    弹幕 WebSocket 抓取
├── danmaku_proto.py       (900)    抖音 protobuf 协议编解码
├── subtitle.py            (562)    弹幕 → .ass 字幕与时间对齐
├── nas.py                 (784)    SMB 挂载 + 归档队列
├── preview.py              (94)    头像/预览图抽帧与管理
├── retention.py           (368)    孤儿目录 30 天延迟清理 ★新增
├── migrate.py             (167)    项目搬家时重写绝对路径 ★新增
├── health.py              (214)    ★组件心跳登记与判定（系统健康面板数据源）
├── preflight.py           (172)    启动自检
├── link_resolver.py       (138)    分享链接 → 主播 ID
├── collect_lightgift.py   (209)    礼物数据收集（辅助）
├── diag_gift.py           (133)    礼物诊断（辅助）
│
├── config.json                      运行时配置（唯一真源，见 §5）
├── config.example.json              配置样例
├── hotspots.json                    热点窗口（实时层）
├── hotspot_events.json              开播事件日志（分析层，保留 7 天）
│
├── recordings/                      ★录制产出根（本地暂存）
│   ├── <主播名>/<日期>/<名字>-<时间戳>-%03d.flv
│   ├── <主播名>/<日期>/.meta/*.danmaku.jsonl
│   └── history.json                 ★全量录制历史（365 条，含归档状态）
├── previews/<主播名>/*.jpg           头像/预览图
├── spool/
│   ├── pending.json                 NAS 归档队列（未完成条目）
│   └── retention.json               孤儿目录清理台账
├── logs/
│   ├── monitor-<YYYY-MM-DD>.log     业务日志（按天轮转）
│   ├── launchd.out.log / .err.log   launchd 标准输出/错误
├── auth/                            登录态缓存（cdp.json / meta.json / storage_state.json）
├── scripts/                         运维与一次性脚本
│   ├── ctl.py                       ★终端快捷指令实现（douyin status/start/stop/restart…），命令清单唯一真源
│   ├── install_cli.sh               ★安装/修复终端入口与 zsh 别名（搬家后重跑）
│   ├── check_docs_sync.py           ★文档漂移检查（代码改了文档没改时报警，已纳入单测）
│   ├── sync_github.py               ★月度同步到 GitHub（先扫敏感内容，命中即中止；再提交 + 走 API 推送）
│   ├── backfill_*.py、purge_bogus_gaps.py、danmaku_sign.js、verify.sh
├── tests/                           23 个测试文件，全套 456 项
├── static/index.html                单文件前端
├── agents/                          历史开发契约文档（00~04）
│
├── recordings_backup_20260825/      ⚠ 84G 历史备份，待人工评审
├── ass_backup_20260825/             ⚠ 含 43 个 NAS/本机均无的唯一副本，待评审
│
├── setup.sh                         建 venv / 装依赖 / 生成 plist 注册 launchd
├── 一键启动.command                 ★双击即用：判活启动 + 打印命令速查表（渲染自 ctl.py）
├── requirements.txt
├── migrate.py / retention.py
└── 文档：README.md、PROJECT_GUIDE.md、AI_AGENT_GUIDE.md、项目整理与迁移方案.md、
    录制-字幕-礼物对接说明书.md（对外对接用：录制/字幕/礼物三条链路的字段与接口明细）、本文件
```

**不在项目内的外部位置**

| 路径 | 用途 |
|---|---|
| `~/DouyinArchive/直播回放/<主播>/<日期>/` | NAS 归档目标（SMB 挂载点，`//<NAS用户名>@<NAS主机>/<共享名>`） |
| `~/Library/LaunchAgents/com.douyin.monitor.plist` | 开机自启、KeepAlive 拉活 |
| macOS 钥匙串 `douyin-cookie` | 抖音登录 Cookie（有效期至 2026-11-03） |
| macOS 钥匙串 `douyin-nas` | NAS SMB 密码 |

---

## 3. 模块职责与边界

### 3.1 核心层

| 模块 | 职责 | 明确不做（边界） |
|---|---|---|
| **monitor.py** | 进程入口与调度中枢：加载配置 → 起 Web UI → 为每个主播起一个 `supervise` 协程 → 起后台巡检任务（NAS 归档、每日热点分析、弹幕巡逻、孤儿清理、录制看门狗、登录态巡检） | 不做具体协议实现；所有 I/O 细节下沉到各模块 |
| **detect.py** | 判断某主播是否开播。通道 A：轻量 HTTP 302 探测；通道 B：`webcast/room/web/enter/` API 确认 | 不做重试策略与频率控制（由 `schedule.py` + `cookie_guard.py` 负责） |
| **schedule.py** | ①热点窗口存储（每位主播最多 2 个历史开播高发时段）；②开播事件日志；③按冷/热/在播三档给出下次轮询间隔 | 不发起任何网络请求 |
| **cookie_guard.py** | 裁决"本次请求能不能带 Cookie"。三道闸门：场景门（非热点时段不用）、频率门（全局最小间隔 / 每主播间隔）、配额门（时配额 60 / 日配额 300） | 不存储 Cookie |
| **auth.py** | 登录态获取与续期：扫码登录（Playwright）、钥匙串读写、定期巡检、失效告警。**严格只服务检测通道** | ❌ 绝不把登录态带进弹幕 WebSocket（抖音限制同账号只能进一个直播间，会互相踢掉正在录制的场次） |

### 3.2 录制与内容层

| 模块 | 职责 | 边界 |
|---|---|---|
| **monitor.py 录制段** | 拉流：拼 ffmpeg 参数、起子进程、分片（默认 2300s）、卡死检测（90s 无数据）与重启（最多 4 次）、下播收尾 | — |
| **danmaku.py** | 匿名身份连接 `webcast/im/push/v2`，抓弹幕/礼物/进场，写 `.danmaku.jsonl`（服务器时间 + 本地接收时间双时间戳） | **失败静默重试 3 次后放弃，绝不影响录制链路** |
| **danmaku_proto.py** | protobuf 编解码 + 签名（调用 `scripts/danmaku_sign.js`） | 纯协议层，无状态 |
| **subtitle.py** | jsonl → .ass。每个视频分片独立锚定时间起点，断链重连不传播错位；支持 `align.json` 人工微调 `global_offset` / `per_segment`；**弹幕与礼物偏移各自独立**（`gift_global_offset` / `gift_per_segment` 叠加在弹幕偏移之上，默认 0 即同步） | 不参与录制 |
| **preview.py** | 从录制流抽帧做头像/预览图，存 `previews/<name>/`，提供选中/删除/上传 | 不写 history |

### 3.3 归档与清理层

| 模块 | 职责 | 边界 |
|---|---|---|
| **nas.py** | SMB 挂载（`ensure_mount`）、主机发现与自动恢复（`discover_nas_host`）、归档队列（`enqueue`/`process_queue`，失败重试 5 次）、挂载点巡检（`nas_watch`） | 只搬文件，不判定"该不该录" |
| **retention.py** ★ | 孤儿主播目录的延迟清理：主播卡片删除后登记台账，满 **30 天**且**所有历史条目已归档到 NAS** 才真正删除本地目录；主播被加回来自动撤销 | ❌ **永不触碰 NAS / 外接盘**；只清理 `BASE` 下 `previews/`、`recordings/` 一级子目录，且经 realpath 校验拒绝符号链接与挂载点 |

### 3.4 交互与运维层

| 模块 | 职责 |
|---|---|
| **webui.py** | 内嵌 HTTP 服务，提供配置读写、状态展示、文件目录跳转、服务启停、登录管理、预览图管理、弹幕字幕校正、组件健康与终端指令清单（`/api/health`、`/api/shortcuts`） |
| **scripts/ctl.py** ★ | 终端快捷指令：`douyin status/start/stop/restart/health/log/open/doctor/help`。与 Web 按钮**共用** `webui._service_plan()` 的执行计划（次序规则只写一遍），额外提供「逐步同步执行 + 轮询校验 + `-n` 干跑 + 环境自检」。`COMMANDS` / `ZSH_ALIASES` 两张表是命令清单的唯一真源（argparse 帮助、`douyin help`、启动窗口速查表同源） | ❌ 不接受任何来自命令行的路径/label 参数，只操作本项目自己的固定 label |
| **scripts/install_cli.sh** ★ | 写 `~/.local/bin/douyin` 入口 + `~/.zshrc` 带标记别名片（幂等、可 `--uninstall`）。是快捷指令安装过程的唯一真源，项目搬家后重跑即修复 |
| **scripts/sync_github.py** ★ | 月度同步到 GitHub 公开仓库：先扫敏感内容（禁词集由 `config.json` 反推：主播昵称 / sec_uid / 直播间号 / NAS 主机与共享名 / 内网网段），命中即中止；有改动则 `git add -A` + 提交；**无改动也要走一遍推送**（上次可能「提交成功但推送失败」，工作区干净不代表不需要推） | ❌ 脚本内不硬编码任何真实值（`tests/test_sync_github.py::TestScriptHygiene` 拦）；❌ 扫描命中时宁可不推 |
| **preflight.py** | 启动时检查依赖/ffmpeg/NAS/Cookie/防休眠，输出 `{id,title,status,detail,action}` |
| **migrate.py** | 项目搬家后，把 `history.json`、`config.json` 等文件里的旧绝对路径前缀批量重写为新根（写前自动生成 `.bak-migrate`） |
| **setup.sh** | 建 venv、装依赖、生成并注册 launchd plist |
| **health.py** ★ | 组件健康心跳登记与判定，是前端「系统健康」面板的唯一数据源。常驻协程每轮 `beat(key)`，`snapshot()` 按宽限阈值出分组快照（ok/warn/down/idle）；进程层与外部依赖由 `webui._health_overrides()` 现算后注入 | ❌ **不做任何自愈动作**（不重启、不重连、不写日志）——健康检查本身不应成为新的故障源。宽限期按最坏情况取，宁可迟报不误报 |

---

## 4. 核心数据流

### 4.1 主链路：从"检测"到"归档"

```
[schedule] 给出下次轮询间隔
     │
     ▼
[detect] 302 探测 ──命中──▶ [detect] room API 确认 ──确认开播──▶
     │                                                        │
     │  (是否带 Cookie 由 cookie_guard 裁决)                   ▼
     │                                          monitor._start_recording()
     │                                                        │
     │                                          ┌─────────────┴─────────────┐
     │                                          ▼                           ▼
     │                                   ffmpeg 拉流写 FLV          danmaku.py 抓弹幕
     │                                   recordings/<name>/<date>/  └▶ .meta/*.jsonl
     │                                          │
     │                                   _history_add() ─▶ recordings/history.json
     │                                   (archive.state = "none")
     ▼
[下播] _stop_recording() → _history_finish()
     │
     ▼
nas.enqueue() ─▶ spool/pending.json ─▶ nas.process_queue() ─▶ 搬到
                                        ~/DouyinArchive/直播回放/<name>/<date>/
     │
     ▼
_on_archive_result() 回写 history.json: archive.state = "archived", dest = NAS 路径
     │
     ▼
subtitle.py 用 .meta jsonl 生成 .ass（可选，按需触发）
```

### 4.2 录制历史状态机（`recordings/history.json`）

每条记录形如：
```json
{"name":"主播A","started_at":1787212747.57,
 "output_path":"/Users/a/Downloads/douyin-monitor/recordings/主播A/主播A-20260820-155907.flv",
 "status":"done","ended_at":...,"duration":18.6,"size":309758,
 "archive":{"state":"archived","dest":"~/DouyinArchive/直播回放/主播A/2026-08-20","target":"nas","at":...,"error":null}}
```

```
none ──(入归档队列)──▶ pending ──(搬运成功)──▶ archived
  │                       │
  │                       └──(重试 5 次仍失败)──▶ failed
  └──(启动时重新定位发现文件不在本机、归档目标里也找不到)──▶ missing
```

当前分布（365 条）：`archived 281 / none 52 / missing 32`。
> ⚠️ 32 条 `missing` 是 8 月 20 日前后的老录制，本机与 NAS 均已无副本，**迁移前即为该状态**（已在迁移前备份中核对），属历史遗留，不影响新录制。

### 4.3 孤儿目录清理（retention）

```
webui 删除主播卡 ──(事件触发)──▶ retention.mark()  ─┐
定时巡检(6h) 扫到孤儿目录 ──────▶ retention.mark()  ─┤
                                                    ▼
                                    spool/retention.json 台账
                                    {"<name>": {"marked_at": ts}}
                                                    │
                      ┌───────────────────────────┴───────────────────────────┐
                      ▼                                                       ▼
        主播被重新添加 → 撤销登记（幂等）                      满 30 天 + 到期
                                                                              │
                                                   安全校验：history 中该主播条目
                                                   全部 archived 才放行；任一
                                                   none/pending/failed → 跳过
                                                                              │
                                                                              ▼
                                                   删除本地 previews/<name>、recordings/<name>
                                                   （NAS 副本不动），成功后从台账移除 → 幂等
```

触发方式：**事件（WebUI 删卡）+ 定时（6 小时巡检）双通道**。
手动执行：`.venv/bin/python retention.py --status` / `--sweep`（默认 dry-run）/`--sweep --apply`。
> 若某主播存在 `missing` 状态条目（本机与 NAS 都找不到文件），默认会**阻塞**清理以防误判（NAS 临时掉线可能被误判）。确认无害后手动加 `--allow-missing` 放行。当前 `主播B`（18 条：4 archived + 14 missing）即处于此状态。

### 4.4 后台常驻任务（`monitor.py` main）

| 协程 | 周期 | 职责 |
|---|---|---|
| `reconcile` | 常驻 | 按 `monitors` 增删主播的监控协程，配置热更新 |
| `supervise`(×N) | 常驻 | 每个主播一个：`run_streamer` 检测 +  supervise 异常重启 |
| `nas_watch` | 常驻 | 挂载点存活巡检 + 归档队列驱动 |
| `daily_analysis_loop` | 每日 05:00 | 热点窗口全量重算（90min 环形聚类 + 近 3 天加权） |
| `danmaku_patrol_loop` | 周期 | 弹幕连接掉线重连 |
| `_recording_watchdog` | 20s | 发现"进程活着但没数据"的假在线录制 |
| `retention_loop` | 6h（启动先跑一次） | 孤儿目录登记与到期清理 |
| `auth.patrol_loop` | 12h ± 2h | 登录态有效性巡检 |

---

## 5. 关键配置项（`config.json`）

| 分组 | 键 | 当前值 | 说明 / 改动风险 |
|---|---|---|---|
| `detection` | `mode` | `mix` | 双通道混合检测 |
| | `cookie_ref` | `keychain:douyin-cookie` | Cookie 只从钥匙串取，配置里不留明文 |
| | `cookie_guard.*` | 见下 | 风控护栏：`hotspot_only=true`、最小间隔 45s、每主播 300s、时配额 60、日配额 300 |
| `auth` | `enabled` / `check_interval_hours` | `true` / `12` | 登录态巡检周期 |
| `schedule` | `cold_interval` | `1800` | 非热点时段轮询间隔（秒） |
| | `hot_min`/`hot_max` | `300`/`360` | 热点窗口内轮询间隔 |
| | `live_interval` | `1200` | 在播时的状态复核间隔 |
| | `hotspot_max` / `hotspot_merge_hours` | `2` / `5` | 每位主播保留的热点窗口数与合并阈值 |
| | `analysis_hour` / `analysis_cluster_min` | `5` / `90` | 每日重算时间与聚类粒度 |
| | `circuit_errors` / `circuit_cooldown` | `5` / `1800` | 连续异常熔断 |
| `webui` | `host` / `port` | `127.0.0.1` / `8780` | 端口可用环境变量 `DOUYIN_MONITOR_PORT` 覆盖 |
| `nas` | `host` / `share` / `mount_point` / `root_dir` | `192.168.1.10` / `share_video` / `~/DouyinArchive` / `直播回放` | `auto_discover=true` 时 IP 变了会自动发现并回写 |
| `archive` | `mode` | `auto` | `auto` 搬 NAS；`external_dir` 非空时搬到指定外部目录 |
| `preview` | `start_delay`/`interval`/`shots` | `60`/`30`/`5` | 开播后 60s 开始抽帧，每 30s 一次，共 5 张 |
| `recorder` | `segment_time` | `2300` | 单个 FLV 分片时长（秒） |
| | `stall_seconds`/`stall_max_restarts`/`stall_cooldown` | `90`/`4`/`600` | 卡死判定与重启上限 |
| `danmaku` | `enabled`/`offset_seconds`/`gift_offset_seconds`/`style` | `true`/`0`/`0`/`queue` | 字幕样式：滚动队列 5 行 / 8 秒；礼物偏移叠加在弹幕偏移之上 |
| `monitors[]` | `auto_record` | 新增主播默认 `false` | **关掉后既不录制，也不检测开播（真休眠）** |
| | `note` | — | 备注，前端点击卡片弹窗编辑 |
| `on_live_command` / `on_offline_command` | — | `echo ...` | 开播/下播回调 shell，支持 `{name}` `{room_id}` 占位符 |

**安全写入规则**：配置一律通过 `_atomic_write_json`（临时文件 + `os.replace`）写入，禁止直接 `open(path,'w')` 覆盖。历史上曾因 `POST /api/config` 整段覆盖 `monitors` 导致 9 位主播被误删——**修改 `monitors` 时只做增量 diff，不要整段替换**。

---

## 6. 功能安全边际（三区划分）

### 🟢 可自由修改区（改坏也能一键回滚，不影响在播录制）

| 范围 | 说明 |
|---|---|
| `static/index.html` | 纯前端，刷新即生效；改前复制一份即可 |
| `webui.py` 的展示类逻辑 | 状态展示、目录跳转、格式化 |
| `preflight.py` 检查项 | 只输出提示，不阻断启动 |
| `scripts/` 下的一次性脚本 | 不参与常驻进程 |
| `docs`（README / PROJECT_GUIDE / 本文件等） | 纯文档 |
| `config.json` 中的展示类与阈值类参数 | `preview.*`、`danmaku.*`、`schedule.*`、`webui.port` |
| `tests/` | 加测试永远安全 |
| `health.py` 的展示层（分组、文案、前端渲染） | 只读快照，改错最多是显示不准，不影响任何业务链路 |

### 🟡 需谨慎改动区（改前停服、改后必须跑全套测试 + 人工观察一轮开播）

| 范围 | 风险点 | 强制要求 |
|---|---|---|
| `monitor.py` 录制/收尾段（`_start_recording`、`_stop_recording`、`_restart_recording`） | 改错会导致漏录、文件截断、进程泄漏 | 停服 → 改 → `python -m unittest discover tests` 全过 → 手动起服观察一次完整开播/下播 |
| `detect.py` | 判定阈值改错会漏播或疯狂误报（误报会烧 Cookie 配额） | 同时看 `cookie_guard` 配额是否被打爆 |
| `schedule.py` 热点算法 | 改错会让所有主播长期处于冷档，漏录 | 观察 `hotspots.json` 是否随开播更新 |
| `nas.py` 归档队列与搬运 | 改错可能**误删源文件**或重复搬运 | 先在 `DRY_RUN=True` 下验证；确认 `_process_one` 的"搬运成功才删源"语义不变 |
| `subtitle.py` | 只影响字幕质量，不影响录制 | 保留 `.meta` jsonl，字幕可无损重生成 |
| `retention.py` | 清理逻辑改错会**误删录制素材** | 永远保持：①只清本地 ②realpath 校验 ③归档完成才删 ④幂等；改动后先 `--sweep`（dry-run）看清单再 `--apply` |
| `config.json` 的 `monitors[]` | 整段覆盖会丢主播 | **只做增量 diff** |
| `health.py` 的宽限阈值（SPECS 的 grace） | 调太小会满屏误报（尤其 6h/12h 周期任务），调太大则故障迟迟不暴露 | 按"轮询周期 + 抖动 + 退避上限 + 余量"取；改完确认重启后 5 分钟内不出现 warn |
| 历史归档状态机（`_mark_archive_state` 等） | 状态写错会让 retention 误判，也可能让已归档文件被重复搬运 | 改完核对 `history.json` 中 `archive.state` 分布不异常 |

### 🔴 禁止改动区（动了会出线上事故）

| 范围 | 原因 |
|---|---|
| **`auth.py` 的"登录态不得用于弹幕/录制"约束** | 抖音限制同账号同时只能进一个直播间，带登录态进弹幕会把正在录制的场次踢掉。本项目靠匿名弹幕实现 4~5 路并发。 |
| **`cookie_guard.py` 的三道闸门主线** | Cookie 是真实账号的登录态，无节制使用会导致限流/封号 |
| **`retention.py` / `nas.py` 中"不碰 NAS、不碰挂载点、不碰符号链接"的护栏** | NAS 是 SMB，**删除不可回滚**（`trash` 对 SMB 无效） |
| **NAS 可写性判定"只以写入成功为准"**（`_probe_writable` / `test_connection`） | 删除探针失败（SMB 回收站、文件占用、安全软件都可能导致）**绝不能**判为不可写：`ensure_mount` 一旦拿到 False 就会 `umount -f` 强行卸载，等于为 5 字节探针炸掉正在归档的卷 |
| **前端「测试连接」不得因探针清理失败报"连接失败"** | 用户会误以为 NAS 挂了，实际卷是好的（2026-09-07 实际踩到） |
| **`_atomic_write_json` 原子写语义**（`monitor.py` L109、`nas.py` L56 等） | 直接覆盖写会在崩溃时留下半截 JSON，导致历史全丢 |
| **`BASE = os.path.dirname(os.path.abspath(__file__))` 的路径无关约定** | 项目整体搬家靠这个约定 + `migrate.py`；任何模块写死绝对路径都会让迁移失效 |
| **环境代理隔离：`_purge_env_proxy()` 与所有 `httpx` 客户端的 `trust_env=False`** | 抖音检测与 ffmpeg 拉流必须直连。去掉任一层，服务从受管终端拉起时会把所有请求送进死代理 → 全员 `All connection attempts failed` → 检测失效、录制停止，而**服务看起来完全正常**（2026-09-09 实际踩到，导致 9-07 起两天半无历史记录） |
| **`history.json` 中已 `archived` 条目的 `dest` 字段** | 这是 NAS 副本的唯一索引，改错就无法定位归档文件 |
| **`spool/pending.json` 的"搬运成功才删源"语义** | 反过来会造成素材丢失 |
| **`recordings_backup_20260825/`（84G）与 `ass_backup_20260825/`** | ⚠️ 待人工评审：`ass_backup` 内含 **43 个 NAS 与本机均无的唯一副本**，评审前**禁止删除** |
| NAS 上的 `.trash_20260901_待删隔离` | 83 个待删文件已隔离改名（可逆），SMB 上无法进回收站，确认无误后再手动清理 |

---

## 7. 运行与运维速查

**终端快捷指令（日常最常用，任意目录可用）**

```bash
douyin help       # ★命令速查：上面全部命令 + 别名 + 作用（--short 为紧凑版）
douyin            # 状态总览：服务 + 健康摘要 + 直播/录制（= dy）
douyin restart    # 重启（= dyr）——会先清掉占用端口的非托管实例
douyin start      # 开启（= dyon）
douyin stop       # 关闭（= dyoff）
douyin health     # 健康面板 14 项逐条（= dyh）
douyin log -f     # 跟随今日业务日志（= dylog）
douyin doctor     # 自检终端环境有无 PYTHONPATH / 代理隐患（= dydr）
douyin open       # 在浏览器打开 Web 控制台
douyin restart -n # 干跑：只打印将要执行的步骤，不真执行（改前先看清楚）
```

**双击 `一键启动.command` 的窗口里会直接打印这张速查表**（`douyin help --short` 渲染），
所以不必记命令 —— 打开就能看到有哪些选项。该表由 `scripts/ctl.py` 的 `COMMANDS` /
`ZSH_ALIASES` 两张表渲染，**是「有哪些命令、各自什么效果」的唯一真源**：argparse 的
子命令、`douyin help`、启动窗口显示的同源，改一处三处一起变（`tests/test_ctl.py::
TestCheatsheet` 守着不许分叉）。启动脚本还会顺手补装丢失的入口（换机/搬家后）。

入口：`~/.local/bin/douyin`（包装脚本，自动清 `PYTHONPATH` 与代理）→ 项目 `scripts/ctl.py`。
搬家或换机后重跑一次安装即可修复入口与别名：`bash scripts/install_cli.sh`（幂等；`--uninstall` 可整体卸掉）。

```bash
# 服务状态 / 重启（launchd）
launchctl print gui/$(id -u)/com.douyin.monitor
launchctl bootout  gui/$(id -u)/com.douyin.monitor
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.douyin.monitor.plist

# 服务启停的规则（唯一定义处）：webui._service_plan()
#   restart —— 先用 _outsider_pid() 判断 8780 是否被非 launchd 实例占用，是则
#     SIGTERM（走 _handle_term 停 ffmpeg、落历史；3 秒未退交给 SIGKILL）再 kickstart。
#   start   —— bootout → bootstrap → kickstart（已装载时裸 bootstrap 会报 already loaded）。
#   stop    —— bootout（job 卸载后必须 bootstrap 才能再起，所以界面上「关闭/启动」成对）。
#   Web 按钮与终端 douyin 命令共用这一份计划；改次序只改这里，两个入口同时生效。
# plist 关键项：KeepAlive=true、ThrottleInterval=60（崩溃重启最小间隔，默认 10 秒
# 会在端口冲突时把 err.log 刷到上百 MB）、ExitTimeOut=30（SIGTERM 后的收尾时间，
# 要停掉所有 ffmpeg 并落历史）。改完 plist 必须 bootout + bootstrap 才生效。

# 月度同步到 GitHub（每月 1 日 21:00 由自动化触发；手动同样可用）
.venv/bin/python scripts/sync_github.py --check     # 只扫描:待提交文件里有没有真实值泄露
.venv/bin/python scripts/sync_github.py --dry-run   # 预览将提交哪些文件、提交说明长什么样
.venv/bin/python scripts/sync_github.py             # 扫描 → 提交 → 推送
# 退出码:0 成功/无改动、1 扫到敏感内容(已中止)、2 推送失败、3 找不到推送脚本
# Agent/自动化环境里 github.com 不可达,脚本自动改走 api.github.com;
# 在你自己的终端里 `git push` 同样可用。

# 手动前台启动（调试用，Ctrl+C 停）
cd /Users/a/Downloads/douyin-monitor && .venv/bin/python monitor.py

# 全部测试（456 项，含文档漂移检查、环境代理隔离检查、健康心跳判定、服务控制计划、终端指令/快捷指令卡片、字段表分组结构、月度同步脚本）
.venv/bin/python -m unittest discover -s tests -v

# 只查文档是否与代码脱节（改完代码建议先跑这个，快）
.venv/bin/python scripts/check_docs_sync.py

# 孤儿清理查看 / 试跑 / 真删
.venv/bin/python retention.py --status
.venv/bin/python retention.py --sweep
.venv/bin/python retention.py --sweep --apply

# 项目搬家（改路径）
.venv/bin/python migrate.py --from <旧根目录> --apply

# 重新注册自启
./setup.sh --reinstall-plist
```

**健康检查清单**
```bash
curl -s --noproxy '*' 127.0.0.1:8780/api/service  # running=true；source=launchd|manual
curl -s --noproxy '*' 127.0.0.1:8780/api/status   # 应返回配置与状态
curl -s --noproxy '*' 127.0.0.1:8780/api/auth     # logged_in 应为 true
curl -s --noproxy '*' 127.0.0.1:8780/api/nas      # mounted=true、backoff_s=0
curl -s --noproxy '*' 127.0.0.1:8780/api/retention # 孤儿清理台账
curl -s --noproxy '*' 127.0.0.1:8780/api/folders  # 目录导航
curl -s --noproxy '*' 127.0.0.1:8780/api/health   # ★组件健康:14 项灯 + 最后心跳
curl -s --noproxy '*' 127.0.0.1:8780/api/shortcuts # ★终端快捷指令清单(健康页卡片的数据)
```

**系统健康面板（`/api/health`）读法**

进程灯全绿但**常驻任务心跳持续变老**，就是"服务活着、业务停了"的典型信号
（2026-09-07 那次失效代理故障就是这个特征）。判定阈值：

| 心跳年龄 | 判定 | 含义 |
|---|---|---|
| ≤ grace | ok | 在预期周期内 |
| grace ~ 2×grace | warn | 延迟，或本轮有异常 |
| > 2×grace | down | 基本可判定卡死 |
| 从未打点 | idle / down | 启动宽限（300s）内为空闲，超时视为故障 |

grace 按最坏情况取（轮询周期 + 抖动 + 退避上限），`detect` 随 `schedule.*_interval` 动态计算。
新增常驻协程时，**记得在循环里 `health.beat(key)` 并在 `health.SPECS` 登记**，否则面板上不会出现。

**终端快捷指令卡片（`/api/shortcuts`）**

同一页下方的「终端快捷指令」卡片，内容现取自 `scripts/ctl.py` 的 `COMMANDS` /
`ZSH_ALIASES`（经 `ctl.cheatsheet()`），前端只负责排版、点命令格可复制。
所以**加一条命令只需改 ctl.py 那两张表** —— 终端 `douyin help`、一键启动窗口、
Web 这张卡片三处同时生效；卡片上写死命令名的话，加了新命令它就会漏。

卡片还会标出 `~/.local/bin/douyin` 装了没：入口在项目外，搬家/换机后最容易失效，
没装时卡片直接给出 `bash scripts/install_cli.sh`。旧服务（未重启）没有这个接口，
前端按 404 降级提示「需重启服务」，不会白屏。
```

> `--noproxy '*'`：本机请求务必绕过系统代理，否则会得到 `000`/超时，误判成服务挂了。

**服务状态判定（2026-09-07 修复 / 2026-09-09 补全）**

`webui._service_status()` **先查 launchd；只要 launchd 说没在跑（不论"未装载"还是"已装载但 state≠running"），都回退按 Web 端口找进程**：

| 场景 | `managed` | `running` | `source` | 前端显示 |
|---|---|---|---|---|
| launchd 托管且存活 | true | true | `launchd` | 服务运行中 |
| 未装载 + 手动拉起 | false | **true** | `manual` | 服务运行中**(未托管)** |
| **已装载但非 launchd 启动**（2026-09-09 补全） | true | **true** | `manual` | 服务运行中**(未托管)**，提示"点重启交给 launchd" |
| 已装载且真的没跑 | true | false | `launchd` | 已装载但未运行 |
| 端口被占但 HTTP 无响应 | — | true（僵死） | — | 一键启动会自动杀掉重启 |

第三种是 2026-09-09 补的：`launchctl print` 能查到 job（`active count = 0`、`state = spawn scheduled`），但那个 job 没在跑，实际进程是手动拉起的 —— 旧代码在 `managed=true` 时盲信 launchd，于是显示"已装载但未运行"，让人以为整个服务挂了（实际录制、弹幕、归档全在跑）。

**环境代理隔离（2026-09-09 修复）**

`monitor.py` 在导入阶段执行 `_purge_env_proxy()`，清除继承来的 `http_proxy` / `https_proxy` / `all_proxy`（大小写都认），并返回清除个数记入启动自检日志。

为什么要这么做：服务若从受管终端 / IDE 集成终端 / CI 拉起，常继承一个指向**已退出的沙箱代理端口**的变量。此时：

- `httpx` 默认 `trust_env=True`，检测请求全被送进死代理 → `All connection attempts failed`
- `ffmpeg` 的 HTTP 拉流同样读 `http_proxy` → 录制也失败
- **服务本身活着、Web UI 照常响应**，唯一症状是「数据不再增长」——极易被忽略

三层防护：

| 层 | 位置 | 作用 |
|---|---|---|
| 环境变量清理 | `monitor.py::_purge_env_proxy()` | 进程最早期清掉，`ffmpeg` 子进程一并受益 |
| 客户端显式关闭 | 所有 `httpx.Client/AsyncClient` 加 `trust_env=False` | 防子模块自建客户端时变量被改回 |
| 启动脚本 | `一键启动.command` 第 0b 步 `unset` | 双击启动天然免疫 |

逃生开关：确实需要走代理时设 `DOUYIN_KEEP_ENV_PROXY=1`。

> 排查同类故障：`grep -c "All connection attempts failed" logs/monitor-<日期>.log`。若全员异常且服务响应正常，先查进程是否继承了代理变量（`ps eww -o command -p <pid> | tr ' ' '\n' | grep -i proxy`）。

---

## 8. 已知遗留事项

1. **服务已回到 launchd 托管（2026-09-11 修复，原第 1 条遗留事项已消除）**：迁移后服务一度由受管执行环境手动拉起成孤儿进程（pid 24190，PPID=1），长期占用 8780，导致 launchd 每次 spawn 都 `Address already in use` 退出、每 10 秒重试一次（累计 4598 次，err.log 7.5MB），而 Web 界面连的一直是那个旧进程——**点「重启」完全看不出变化，且 9-09 之后的全部改动都没上线**。已结束该进程，现由 launchd 接管（`state = running`、`source=launchd`），plist 增加 `ThrottleInterval=60`。**注意：服务必须由你自己的终端/双击启动**——在受管执行环境里拉起的进程会继承安全钩子，`os.remove` 会被劫持（表现为 `[safe-delete] FSMoveObjectToTrashSync failed`），进而污染 NAS 判定；本次还观察到它会让 `os.remove` 在退出清理阶段阻塞，进程僵死在收尾中（端口还在听但 HTTP 无响应），只能 `kill -KILL`。
2. **32 条 `missing` 历史条目**：8 月 20 日前后的老录制，本机与 NAS 均无副本，属历史遗留。
3. **高风险目录待评审**：`recordings_backup_20260825/`（84G）、`recordings/` 中 52 条 `none` 未归档暂存、`ass_backup_20260825/`（43 个唯一副本）。
4. **孤儿清理首次到期观察**：当前台账 1 条（`主播B`，2026-09-06 登记，**2026-10-06 期满**；因 14 条 missing 被安全校验阻塞）。期满后若确认弹幕无用，先备份再 `--allow-missing` 放行；弹幕已在 `~/Desktop/主播B_弹幕备份_20260907.tar.gz` 留档。
5. NAS 隔离区 `.trash_20260901_待删隔离`（83 个文件）待人工确认后清理。
6. **NAS 共享名别选错**：`192.168.1.10` 上有 4 个共享，**不要选带 `_公共空间` 后缀的**（那是另一条路径），正确的是 `share_video`。归档设置页点「检测共享」后需人工确认。
7. **跑单测会往正式日志里写字**：`LOG_DIR` 是项目内固定路径，unittest 中的 `主播A` 等构造数据会追加进 `logs/monitor-<当天>.log`。不影响功能，但看日志时要能分辨（`grep` 时排除 `主播A` 即可）。
8. **测试不要在项目根落临时文件**：根目录的 `.md` 会被 `check_docs_sync._docs()` 收进检查范围，落盘再删易残留并污染真实结果（`os.remove` 在受管环境下可能被拦）。需要构造文档内容时用打桩。
9. **2026-09-07 04:03 ~ 2026-09-09 16:40 的录制已永久缺失，不可追回**：该时段服务继承了失效的环境代理，全员检测失败（详见 §7）。history 里这段是空白，不是清理策略造成的。已加三层代理隔离 + 系统性故障告警防止重演。
10. **点「重启」没反应时，先查端口归属（2026-09-11 事故总结）**：`kickstart -k` 只作用于 launchd 名下的进程。若 8780 被一个不在名下的实例占着（手动拉起/上次残留），新实例一 bind 就退出、KeepAlive 又立刻重拉，形成每 10 秒一次的崩溃循环，而界面始终连着旧实例、看不出任何变化。判定四连：`launchctl print` 看 `state / runs / last exit code` → `lsof -tiTCP:8780 -sTCP:LISTEN` 看端口归谁 → 对比端口 pid 的启动时间与代码 mtime（判断跑的是不是旧代码）→ `curl --noproxy '*' .../api/health` 返回 404 即证实旧代码。代码侧已加固：`webui._outsider_pid()` 识别这类外来户，重启时先 `SIGTERM`（走 `_handle_term` 停 ffmpeg、落历史）再 `kickstart`；`start` 改为先 `bootout` 再 `bootstrap`，避免已装载时报 already loaded。终端里对应动作是 `douyin restart`（含清场），`douyin status` 会在端口被外来实例占用时直接点出来。

---

## 9. 文档同步约定（改动者必读）

**任何代码 / 配置改动，都必须同步更新根目录文档。** 历史教训：2026-09-07 项目搬家改了路径、服务状态判定和 NAS 共享名之后，`AI_AGENT_GUIDE.md` 里仍写着旧项目根，接手的人（或另一个智能体）按文档走会直接走到错误的目录。

### 9.1 谁该在什么时候更新

| 文件 | 触发更新的改动 |
|---|---|
| `项目框架说明书.md`（本文件） | 模块增删、API 增删、配置项增删、数据流/状态机变化、安全边际变化、测试数变化 |
| `项目整理与迁移方案.md` | 目录结构变化、项目根路径变化、保留/清理清单变化、迁移相关经验、换机/搬家流程步骤 |
| `AI_AGENT_GUIDE.md` | 项目根路径、端口、模块清单、启动方式、测试数 |
| `README.md` / `PROJECT_GUIDE.md` | 依赖变化、启动方式变化、目录结构变化、快捷指令变化 |
| `录制-字幕-礼物对接说明书.md` | 录制/弹幕/礼物/字幕的**数据格式与字段约定**变化：jsonl 行结构、align.json 字段、ffmpeg 参数、history 条目、路径推导规则 |

**六条易漏的纪律**：

- 改服务启停规则时，只改 `webui._service_plan()`。Web 按钮与终端 `douyin` 命令共用它，各自另写一份必然分叉（`tests/test_ctl.py::TestControlSharesPlan` 就是拦这个的）。
- 增删命令时，只改 `scripts/ctl.py` 的 `COMMANDS` / `ZSH_ALIASES` 两张表。argparse 帮助、`douyin help`（终端/`--json`）、`一键启动.command` 窗口、Web「系统健康」页的快捷指令卡片都从它们渲染；含前端在内的任何消费方都只负责「显示」，不许抄一份命令表（`tests/test_ctl.py::TestCheatsheet` 校验终端与启动窗口，`tests/test_shortcuts_api.py` 校验接口内容与真源逐字一致、且前端静态 HTML 里没有写死的命令表）。
- `~/.local/bin/douyin` 由 `scripts/install_cli.sh` 生成，**不要直接手改那个入口文件**（它在项目外，下次重跑安装就被覆盖）。要改行为改 `scripts/ctl.py`，要改入口改 `install_cli.sh`。
- 新增 Web 接口时，记得在说明书的健康检查清单（§7）里登记一行 —— 现在 `check_docs_sync.py` 会反向核对：说明书里写到的 `/api/xxx` 必须在 `webui.py` 的路由里真实存在。
- 改「高级参数 → 监控与开播检测」的字段时,只改 `static/index.html` 的 `ADVANCED_GROUPS` 表 —— 卡片里的分组、组头说明、组内字段、默认展开态全由它驱动(渲染、折叠记忆、一键全展开都从它读)。加字段/改标签/挪分组都只动这一处;`tests/test_advanced_groups.py` 会拿它跟后端 `monitor.DEFAULT_CONFIG` 双向对账(后端登记的可配键一个都不许漏、表里的路径必须真实存在、不得与「常用参数」卡重复)。
  另外:折叠**只切 class、不摘 DOM** 是刻意的 —— 收起组里的字段仍要参与 `loadConfig` 回填与 `collectFields` 收集,若改成移除节点,一按「保存并应用」就会把收起组的键静默清空。
- 月度同步只走 `scripts/sync_github.py`(自动化每月 1 日 21:00 调它)。**别在这个脚本里硬编码主播昵称、sec_uid、内网地址** —— 禁词集是从 `config.json`(已被 `.gitignore` 排除)反推的,加主播/换 NAS 会自动跟上;硬编码一份就会在配置变动时静默失效。`tests/test_sync_github.py::TestRealRepoIsClean` 是守门人:待提交文件集里出现任何真实值,跑测试就红。

### 9.2 自动化兜底

`scripts/check_docs_sync.py` 把「文档里写的事实」与「代码真实状态」做比对，并已纳入单测（`tests/test_docs_sync.py`）。**改完代码跑一次全套测试即可发现文档漂移**：

```bash
.venv/bin/python scripts/check_docs_sync.py     # 单独跑,几毫秒
.venv/bin/python -m unittest discover -s tests  # 全套(含该项)
```

能自动检出：

| # | 检查项 |
|---|---|
| ① | 文档里残留迁移前的旧项目根（标注为「迁移历史/旧路径」的行除外） |
| ② | 声明的项目根 ≠ 真实 `BASE` |
| ③ | 文档里写的 `127.0.0.1:<端口>` ≠ `config.json` 的 `webui.port` |
| ④ | 根目录出现说明书**未登记**的新 `.py` 模块 |
| ⑤ | 说明书登记了**不存在**的模块 |
| ⑥ | 说明书里写到的 `/api/xxx` 在 `webui.py` 里**不存在**（模板占位写法如 `/api/xxx`、`/api/<主播>` 不算；反向不查：说明书是速查性质、未逐一登记全部 POST 路由，硬查会大量误报） |

**检不出的（只能靠人）**：模块职责描述过时、数据流/状态机变化、安全边际变化、配置项语义变化。这些改动请同步改 §3 / §4 / §6。

### 9.3 建议的改动收尾顺序

1. 改代码 → 2. 跑 `check_docs_sync.py` → 3. 按提示更新文档 → 4. 跑全套测试 → 5. 重启服务并做 §7 的健康检查。
