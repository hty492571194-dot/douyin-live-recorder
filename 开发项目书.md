# 抖音直播监控录制系统 — 开发项目书

> 版本 v1.0 · 2026-08-16 · 部署目标:macOS 26 + Apple Silicon(M4)

---

## 1. 项目概述

### 1.1 背景
需要对抖音平台多个主播(4~5 路)进行直播的自动监测与录制存档,并将素材归档到 NAS。核心矛盾在于:抖音反爬严格、流地址与房间号频繁变化,需要在「录得全」和「不被风控」之间取得平衡。

### 1.2 目标
1. 前端录入要跟踪的主播,系统**自动监测开播状态**,开播即自动录制、下播自动归档。
2. 监测与录制全程对反爬友好:双通道检测、错峰、自适应轮询、账号隔离、熔断退避。
3. 录制成品按「主播名」分目录归档到 NAS,并保留从属关系元数据。

### 1.3 范围
- 含:开播监测、录制触发、热点调度、Web 控制台、参数热更新。
- 不含(依赖既有组件):实际拉流录制由 `DouyinLiveRecorder`(+WebUI)承担,锚点库由既有 UID 工具提供。

---

## 2. 需求说明

### 2.1 本次调整项(核心需求)

| # | 需求 | 说明 |
|---|---|---|
| 1 | **双通道混合检测** | 开播检测用「302 重定向探测 + room status API 确认」两种方式混合,任一失效自动回退 |
| 2 | **热点上限 2 个 + 前端可调** | 每主播最多保留 2 个热点时间窗;Web UI 需能**展示并调整**全部调度参数 |
| 3 | **录制中轮询降频到 20 分钟** | 录制期间轮询仅作冗余兜底(见 5.3),间隔改为约 20 分钟 |

### 2.2 功能需求
- F1:前端录入主播(链接/锚点),自动解析主播名。
- F2:错峰轮询开播状态,开播触发录制命令,下播触发归档命令。
- F3:自适应两段式轮询(冷门 30min / 热点 5~6min / 录制中 20min)。
- F4:Web 控制台展示主播状态 + 调整参数,保存即时生效。
- F5:双通道检测,失败自动回退与熔断。

### 2.3 非功能需求
- 反爬友好:请求错峰、间隔随机抖动、熔断退避。
- 可持久化:配置与热点窗口落盘,重启不丢。
- 可观测:日志 + 状态快照。
- 部署:macOS 原生运行(标准库 + httpx,无 Docker)。

---

## 3. 系统架构

```
前端 Web UI(static/index.html)
        │  GET/POST /api/*
        ▼
内嵌 HTTP 服务(webui.py ── State 配置热更新)
        ▲ 状态快照
        │
监控主循环(monitor.py)
 ├─ 检测层(detect.py):302 探测 + API 确认(mix)
 ├─ 调度层(schedule.py):热点窗口 + 自适应间隔
 └─ 触发层:on_live_command / on_offline_command → 录制器
```

---

## 4. 技术选型

| 组件 | 选型 | 理由 |
|---|---|---|
| 语言/运行时 | Python 3.11+ / asyncio | 与既有脚本一致,异步并发监测多主播 |
| HTTP 客户端 | httpx(异步) | 302 探测需关闭自动重定向 |
| Web UI | 标准库 http.server + 原生 HTML/JS | 零重依赖,内嵌即可 |
| 持久化 | JSON(config.json / hotspots.json) | 简单、可读、易改 |
| 录制器 | DouyinLiveRecorder(+WebUI) | 现成方案,复用 |

---

## 5. 核心设计

### 5.1 主播锚定信息(稳定 vs 易变)

| 标识 | 稳定性 | 用途 |
|---|---|---|
| `sec_uid`(`MS4wLjAB...`) | **最稳定(用户唯一 ID)** | 主锚点 |
| `web_rid`(`live.douyin.com/{数字}`) | 较稳定(账号级) | 备选锚点 |
| `room_id`(`/reflow/{数字}`) | **每场开播都变** | 录制取流用(实时抓取,不作锚) |

结论:**跟踪主播用 sec_uid / web_rid,不用 room_id**。检测到开播的瞬间,从 302 重定向地址里实时抓取当次 room_id 交给录制器。

### 5.2 双通道混合检测(detect.py)

| 通道 | 原理 | 特点 |
|---|---|---|
| 302 探测 | `GET live.douyin.com/{anchor}`(移动 UA、不跟随重定向):302=开播,200=未开播 | 轻量、无需签名、风控风险低 |
| API 确认 | `webcast/room/web/enter`(带 Cookie,可能需签名)返回 `status`(2=直播) + `stream_url` | 信息全、可直接拿流地址,但请求重 |

**混合策略(`mode=mix`)**:
1. 每轮先跑 302(廉价)。
2. 302 判定开播/异常时,用 API 二次确认并取 `stream_url`。
3. API 失败 → 自动回退信任 302 结果。
4. 可选 `mode=302`(仅 302)或 `mode=api`(仅 API)。

> 说明:API 通道需要有效 Cookie(部分接口还需 X-Bogus 签名),失败自动回退,不影响 302 主链路。

### 5.3 自适应轮询调度(schedule.py)

**录制中轮询的目的**:原为检测"下播"以触发归档。但录制器自身(ffmpeg 收流 EOF / Recorder 循环值守)会检测到流结束并停录,故此阶段轮询仅为冗余兜底,已降频为 **20 分钟**。

| 阶段 | 间隔 | 抖动 |
|---|---|---|
| 冷门(未知时间表) | **30 分钟** | ±20%(24~36min) |
| 热点窗口内 | **5~6 分钟** | uniform 300~360s |
| 录制中 | **20 分钟** | ±20%(16~24min) |
| 熔断冷却 | 默认 30 分钟 | 连续 5 次异常触发 |

**热点窗口规则**:
- 检测到开播 → 记录 `true_start = 检测时刻 - 上次间隔` 为窗口中心,窗口半宽默认 ±30 分钟。
- 中心相距 ≤45 分钟合并;**每主播最多保留 2 个**。
- 14 天未再命中 → 淘汰(应对主播改时间)。
- 跨主播启动随机相位,避免同刻并发。

### 5.4 参数可调(config.json,Web UI 热更新)

全部参数见 §6,前端通过 `POST /api/config` 热更新,无需重启。

---

## 6. 配置设计(config.json)

```json
{
  "detection": { "mode": "mix", "cookie": "", "api_base": "..." },
  "schedule": {
    "cold_interval": 1800, "cold_jitter": 0.2,
    "hot_min": 300, "hot_max": 360, "live_interval": 1200,
    "hotspot_half_width": 1800, "hotspot_max": 2,
    "hotspot_merge": 2700, "hotspot_decay_days": 14,
    "circuit_errors": 5, "circuit_cooldown": 1800
  },
  "webui": { "host": "127.0.0.1", "port": 8780 },
  "monitors": [ { "name": "示例主播", "anchor": "2145431900" } ],
  "on_live_command": "...", "on_offline_command": "..."
}
```

---

## 7. 接口设计(内嵌 Web UI)

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/` | 控制台页面 |
| GET | `/api/config` | 当前配置 |
| POST | `/api/config` | 合并更新并保存(热生效) |
| GET | `/api/status` | 主播状态快照(阶段/热点数/下次检测/检测方式) |

---

## 8. 开发任务分解

| 模块 | 文件 | 说明 |
|---|---|---|
| 双通道检测 | `detect.py` | 302 + API mix,自动回退 |
| 自适应调度 | `schedule.py` | 热点窗口 + 间隔计算 |
| 内嵌 Web UI | `webui.py` + `static/index.html` | 展示 + 调参 |
| 主程序 | `monitor.py` | 串联循环 + 触发命令 |
| 配置/文档 | `config.json` / `README.md` | 默认参数 / 运行说明 |

---

## 9. 验收标准

- [x] 配置多个主播后能并发错峰轮询。
- [x] 开播触发 on_live_command、下播触发 on_offline_command。
- [x] 热点窗口每主播 ≤2 个,14 天衰减。
- [x] 录制中轮询间隔 ≈20min。
- [x] Web UI 可改参数并即时生效。
- [x] 检测通道任一失效自动回退,连续异常熔断。

---

## 10. 风险与应对

| 风险 | 应对 |
|---|---|
| 抖音反爬/接口改版导致检测失效 | 双通道互备、紧跟工具版本、熔断退避 |
| 同 IP 多主播风控 | 错峰 + 随机抖动 + 一号一路(账号由录制器管理) |
| room_id 每场变化 | 开播瞬间实时抓取,不持久化 room_id |
| 主播改名/换号 | sec_uid 为稳定主键,热点按期衰减重建 |

---

## 11. 部署(macOS M4)

```bash
pip install httpx
python3 monitor.py        # 启动监控 + 控制台
# 打开 http://127.0.0.1:8780
```

防休眠(无人值守必需):`caffeinate -dimsu` 常驻 + 关闭系统自动更新。
