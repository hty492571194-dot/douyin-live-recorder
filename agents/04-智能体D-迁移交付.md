# 子任务书 D — 迁移交付（阶段 D）

> 你是发布工程智能体。只允许读：本文件、`agents/00-接口契约.md`、`README.md`、`setup.sh`、目录结构（`ls -R` 一层即可）。
> 预计上下文消耗：~30k tokens。基于 `phase-c` 开工，完成打 `phase-d`。这是收尾阶段，代码改动最少。

## 任务

**D1 scripts/pack.sh**：产出搬迁包 `dist/douyin-monitor-<git describe>.tar.gz`，排除 `.venv/ logs/ __pycache__/ spool/ .git/`（保留 hotspots.json 与 config.json 作为可选模板：config.json 若含个人 Cookie/密码引用则改打包 `config.example.json`，详见 D3）。

**D2 M4 迁移文档**（README.md 增补「迁移到新 Mac」一节）：
1. 三步部署：解包 → `./setup.sh --launchd` → Web UI 向导（NAS 连接 / Cookie / 主播列表）
2. 注意事项：合盖休眠（caffeinate 或系统设置免睡眠）、隔空传输脚本 `xattr -dr com.apple.quarantine .`、热点历史 hotspots.json 随包携带即开即用
3. 数据位置速查表：配置/热点/日志/暂存队列分别在哪个目录

**D3 隐私清理**：检查 config.json 打包内容——`detection.cookie`、`nas.username` 非空时不得直接打包原值；打包流程将 config.json 转为 `config.example.json`（清空 cookie/username/password_ref，保留结构），原 config.json 不入包。

**D4 总验收执行**：按契约勾选《AI开发任务书.md》§9 验收清单中可自动验证的项并逐条记录结果（挂机 24h 项标注「留给用户」）；在 /tmp 解包演练一遍 D2 三步流程。

## 验收（打 phase-d 前全部满足）

**先在 `scripts/verify.sh` 追加 `# --- phase-d ---` 检查块**（pack.sh 产物内容断言、config.example.json 脱敏断言、/tmp 解包演练），然后统一以 `bash scripts/verify.sh d` 退出码 0 为准。§9 总验收清单中可自动项全部勾选后才打标签。

```bash
bash scripts/pack.sh && tar -tzf dist/*.tar.gz | head -20   # 无 .venv/logs/.git
tar -xzOf dist/*.tar.gz douyin-monitor-*/config.example.json | grep -c cookie   # 结构在、值为空
cd /tmp && mkdir drill && cd drill && tar xzf <包路径> && cd */ && ./setup.sh   # 演练通过
python3 -m unittest discover tests
```

## 禁止

功能性代码改动（发现 bug 回报给协调者，由对应阶段智能体修复后重走标签）；绕过隐私清理直接打包。
