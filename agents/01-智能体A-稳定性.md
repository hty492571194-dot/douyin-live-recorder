# 子任务书 A — 稳定性加固（git 基线 + 阶段 A）

> 你是后端稳定性智能体。只允许读：本文件、`agents/00-接口契约.md`、`monitor.py`、`schedule.py`、`webui.py`、`detect.py`（仅 §任务A1 引用其 check() 签名，不必逐行读）。
> 预计上下文消耗：~60k tokens。基于 `baseline` 标签开工，完成打 `phase-a`。

## 任务

**A0 git 基线**：`git init`；`.gitignore`（`__pycache__/`、`.venv/`、`logs/`、`*.pyc`、`.DS_Store`、`spool/`）；全部现有代码 commit，打标签 `baseline`。

**A1 协程守护**（修复 `monitor.py:153` gather 单点崩溃）：
- 新增 `supervise(mon)` 包装：`while True: try run_streamer except → log + 指数退避(1→2→4→8→10min 上限)重启`；退避中 `state.set_streamer(name, phase="异常重启")`。
- gather 改为 supervise 集合；顶层兜底 log 后 re-raise。

**A2 配置校验 + monitors 动态生效**（修复 webui.py 无校验、UI 增删主播不生效）：
- `update_config` 按契约 §2 改签名与校验：schedule 数值必须是 number 且 >0，interval 下限 30s，`hot_min<=hot_max`，`hotspot_max∈[1,5]`，`decay_days>=1`，`mode∈{302,api,mix}`；webui.host/port 变更 → restart_required=True。
- `State.monitors_version` 计数（契约 §2）。
- `monitor.py` 新增 `reconcile` 协程：每 5s 比对运行任务集合（key=name+anchor）与 config.monitors，增启删取消；取消时若直播中先触发 on_offline_command。

**A3 原子写 + 损坏自愈**（webui.py `_save` 与 monitor.py `load_config`）：tmp + `os.replace`；读到损坏 JSON → 备份为 `config.json.corrupt-<ts>` → 重建默认 → 告警不崩溃。

**A4 跨零点修复 + 单测**（schedule.py:58）：合并前把 minute 按环形距离解到 center 同侧（差>720 则 ±1440），平均后 mod 1440。新建 `tests/test_schedule.py`（unittest）覆盖：跨零点合并、常规合并、上限淘汰、decay。可 `python3 -m unittest tests.test_schedule`。

**A5 命令可观测**（monitor.py run_command）：记录 `(pid,name,cmd)` 到 State 的 commands 列表，每轮 `poll()`，非 0 退出码告警；`/api/status` 输出 `commands` 键（契约 §3）。

**A6 日志落盘**：monitor.log 同时追加 `logs/monitor-YYYY-MM-DD.log`，跨天切分，清理 7 天前，写失败降级 stdout。

**A7 机械验收门脚本**：创建 `scripts/verify.sh`，用法 `bash scripts/verify.sh a|b|c|d`，子命令对应阶段，结构：
```bash
# 公共：py_compile 全部 py + unittest discover 全量 → 任一失败 exit 1
# 冒烟：后台启动 monitor.py → 就绪等待 → 逐条 curl 断言本阶段 API 契约 → kill 进程
# --- phase-a --- ：POST 坏配置返回 400；/api/status 含 commands 键；config 损坏自愈
# --- phase-b ---（占位注释，B 智能体追加）  --- phase-c ---  --- phase-d --- 同理
```
冒烟启动用随机端口或先杀 8780 占用；脚本必须幂等（重复运行结果一致）。本阶段验收改为：`bash scripts/verify.sh a` 退出码 0。

## 验收（全部满足才打 phase-a）

**统一以 `bash scripts/verify.sh a` 退出码 0 为准**（A7 已把下列断言固化进脚本；脚本本身也要先人工跑通一遍确认无误报）。

```bash
python3 -m py_compile monitor.py schedule.py webui.py detect.py
python3 -m unittest discover tests          # 全绿
# 冒烟：启动后 ——
curl -s -X POST http://127.0.0.1:8780/api/config -d '{"schedule":{"cold_interval":"x"}}'   # 400
curl -s http://127.0.0.1:8780/api/status | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'commands' in d"
```
手工把 config.json 截断 → 重启自愈并产生 .corrupt 备份。手工截断 hotspots.json 同理不崩。

## 禁止

改 detect.py 的协议逻辑/请求头/判定规则；引入第三方库；动 static/index.html（A2 的 400 错误前端 toast 已能显示，无需改）。
