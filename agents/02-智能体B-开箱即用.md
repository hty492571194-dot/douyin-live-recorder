# 子任务书 B — 开箱即用（阶段 B）

> 你是部署体验智能体。只允许读：本文件、`agents/00-接口契约.md`、`monitor.py` 的 `run_command`/`BASE` 相关行段（grep 定位）、`static/index.html`、`README.md`（若存在）。
> 预计上下文消耗：~50k tokens。基于 `phase-a` 开工，完成打 `phase-b`。

## 任务

**B1 {BASE} 占位符**：monitor.py run_command 的 ctx 增加 `"BASE": BASE`（项目根目录绝对路径）。不改默认模板内容。

**B2 preflight 模块**：新建 `preflight.py`，唯一导出 `run_checks(config) -> list[dict]`（结构严格按契约 §3）。检查项：
1. httpx 可导入（fail → action 给 `pip install httpx`）
2. 命令模板含 `/Users/` 或 `/home/` 绝对路径（warn → 建议改 {BASE}）
3. `ffmpeg` 在 PATH（warn）
4. `config.nas.enabled` 为 false（warn → 引导 NAS 面板，action: "待阶段C"）
5. `detection.cookie` 为空（warn：API 通道休眠，mix 退化为 302）
6. 防休眠：进程父链无 caffeinate 且 `pmset -g` 显示会睡眠（warn → 给 caffeinate 命令）
- `webui.py` 增加 `GET /api/preflight`；monitor.py 启动时执行一次打印摘要。
- 6 号检查的 pmset 解析失败按 ok 处理（不误报）。

**B3 前端横幅**（只改 static/index.html）：
- `<main>` 顶部加 `#setup-banner` 卡片：复用 `.card`，加左色条样式（warn #fbbf24 / fail #f87171）；随现有 3s 轮询拉 `/api/preflight`；fail 置顶、全 ok 隐藏；cookie 项点击 `showView('advanced')`。
- 不新增框架/库/构建步骤。

**B4 setup.sh + requirements.txt**：
- requirements.txt 仅 `httpx`。
- setup.sh（可执行）：① 检测 python3 ≥3.11（失败退出并提示）② 建 `.venv` + 装 requirements ③ config.json 不存在则生成默认 ④ `--launchd` 选项生成 `~/Library/LaunchAgents/com.douyin.monitor.plist`（ProgramArguments=[.venv/bin/python, monitor.py]，WorkingDirectory=项目根，KeepAlive，日志重定向 logs/，ProcessType=Background）并 `launchctl load` ⑤ 末尾打印「打开 http://127.0.0.1:8780 完成配置向导」。
- README.md 增加「新机部署」「M4 注意事项（休眠/quarantine）」两节；无 README 则创建。

## 验收（打 phase-b 前全部满足）

**先在 `scripts/verify.sh` 追加 `# --- phase-b ---` 检查块**（preflight API 结构断言、/tmp 新目录 setup.sh 冒烟、{BASE} 替换验证），然后统一以 `bash scripts/verify.sh b` 退出码 0 为准。

```bash
python3 -m py_compile preflight.py monitor.py webui.py
./setup.sh && .venv/bin/python monitor.py &
curl -s http://127.0.0.1:8780/api/preflight | python3 -m json.tool   # 结构符合契约
```
- `rsync` 项目到 /tmp 模拟新目录（排除 .venv/logs/__pycache__），setup.sh 后启动成功，横幅显示 NAS/Cookie 警告。
- 模板中写 `{BASE}` 占位符能被正确替换（echo 模板验证输出）。

## 禁止

改 detect.py/schedule.py/nas 相关逻辑；改 A 阶段已实现的校验/调度行为；引入任何 JS 库。
